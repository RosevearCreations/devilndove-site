name: Release 457 Source Gate — Closed Snapshot

on:
  workflow_dispatch:
    inputs:
      ref:
        description: Historical Release 457 ref to re-prove
        required: true

permissions:
  contents: read

jobs:
  historical-source-snapshot:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v7
        with:
          node-version: '24'
      - name: Re-prove historical Release 457 source
        run: python scripts/release457_financials_operations_gate.py
      - name: Closed snapshot boundary
        run: |
          echo 'Historical Release 457 snapshot only; ordinary dev pushes do not run this workflow.'
          echo 'D1/R2/provider/Production mutation: NONE'
