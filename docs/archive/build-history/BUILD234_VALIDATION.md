# Devil n Dove Build 234 Validation

Build 234 is locally verifiable for source integrity, deterministic rendering/data boundaries, schema repeatability and guarded deletion. Production D1, Cloudflare invocation, printer, laser, material, regulatory and customer-specific evidence must still be recorded in Startup Readiness.

## Recorded local result

- PASS — Build 230 manifest/current Startup, Build 231 autosave/reload, Build 232 archived-product removal, Build 233 bounded login/session and Build 234 packaging/deletion/schema regression scripts.
- PASS — the numbered/current Build 234 migration is byte-identical, contains no explicit SQL transaction statements and applied twice after each of the three aggregate schemas; 44 unique Startup definitions, five adopted references and five new system templates were verified.
- PASS — Deployment Preflight reported Ready with 0 blockers and 0 warnings; predeploy sanity inspected 108 exposed HTML pages with 0 issues; the final deployment blocker check passed.
- PASS — Wrangler 3.114.17 compiled the complete Pages Functions bundle into one Worker.
- PASS — rendered PNG QA showed the botanical rose and wedding candle artwork present; production/SVG defaults use PNG for wider renderer compatibility while WebP variants remain available.
- NOT PROVEN LOCALLY — production D1 state, Cloudflare CPU/memory outcome, live provider operations, measured print/laser output and regulatory/accounting approval.

## Local release checks

1. Run `node scripts/sync-build234-startup-seed.mjs` and `node scripts/sync-build234-aggregate-schema.mjs`; confirm there is no unreviewed drift.
2. Run `node scripts/build230_visual_manifest_test.mjs`, `node scripts/build231_product_autosave_test.mjs`, `node scripts/build232_product_removal_test.mjs`, `node scripts/build233_login_resource_test.mjs` and `node scripts/build234_packaging_creative_test.mjs`.
3. Run `python3 scripts/deployment_preflight_static_check.py`, `python3 scripts/predeploy_sanity_check.py .` and `python3 scripts/final_deployment_blocker_check.py`.
4. Compile the complete Pages Functions bundle. A syntax-only test is insufficient because Pages deploys the functions as one Worker.
5. Confirm `database_upgrade_current_pass.sql` is byte-identical to `database_build234_packaging_templates_creative_cleanup.sql`, contains no explicit `BEGIN`, `COMMIT`, `SAVEPOINT`, `RELEASE` or `ROLLBACK`, and applies twice after each aggregate schema.
6. Confirm exactly 44 unique Startup definitions, five active adopted packaging references and the five new system template keys.
7. Confirm all exposed HTML has one H1, title, meta description and viewport; CSS braces are balanced and changed controls do not overlap at phone/desktop widths.

## D1/deployment procedure

1. Export/back up production D1 and record the previous deployment/rollback ID.
2. Confirm ledger keys `build229_packaging_reference_authority` and `build230_visual_image_manifest` before applying Build 234.
3. Apply either the numbered Build 234 migration or the identical current-pass file once. Do not apply both.
4. Confirm ledger key `build234_packaging_templates_creative_cleanup`, five active packaging references, all five new system templates and 44 active Startup rows. Existing Startup owner/status/evidence fields must remain unchanged.
5. Deploy the complete archive; hard refresh until service-worker shell v15 is active.
6. In Labeling & Packaging, choose each system template and verify that the chosen template—not stale project data—controls the renderer and saved package type.

## Soap proof

1. Open the Glacial Purple soap template and apply its reference defaults.
2. Verify the permanent brand, website, origin, small-batch, claims and weight elements are present; all editable wording is centred where the approved composition requires it.
3. Replace demo ingredient/claim text only with verified source/formula facts; do not infer INCI or legal compliance.
4. Toggle bleed, safe area, folds and glue/overlap; inspect at 100% and narrow/wide screens.
5. Download SVG and confirm the botanical artwork is embedded rather than website-relative.
6. Print at 100%, measure the whole ribbon, band, oval, rear seal, safe/bleed and actual wrap. Resolve the documented 25/38.1/50 mm rear-seal conflict and save the selected profile.
7. Record the approved project version/export checksum/physical print test and applicable Canadian label review before production.

## Candle-top and reusable-template proof

1. Measure an owner-controlled candle lid or laser blank; enter the exact millimetre dimensions instead of assuming the sample is a dieline.
2. Choose the wedding template and replace names, dates and occasion. Verify top/bottom arcs and all straight lines are centred and remain editable live text.
3. Save a custom repeat-job template, reload the page, create a new project from it and verify size, shape, profile, bleed/safe margins, colours and artwork defaults persist.
4. Download SVG and confirm the line artwork is embedded. Treat raster artwork as non-vector until the chosen laser workflow has a reviewed trace/vector source.
5. Run a non-customer material/laser or 100%-scale print proof; verify diameter, safe area, text fit, material compatibility, legibility and fire/placement safety. Archive version, settings, checksum and evidence.

## Creative duplicate-deletion proof

1. Create one disposable untouched Creative Automation shell and one separate project containing meaningful review/evidence/output/history.
2. Preview deletion of the untouched shell. Confirm it lists only automatic initialization records and requires the exact `DELETE <project_key>` phrase.
3. Delete it and verify its real source product was not deleted.
4. Preview deletion of the meaningful project. Confirm the server blocks deletion and reports the protecting work; do not bypass the block.
5. Check the admin audit/runtime incident record and reload the list to confirm one effect only.

## Honest remaining blockers

Passing local checks does not pass Startup gates that require live payment/webhook/refund/inventory concurrency, tax/shipping, email, Meta roles/scopes, backup restore, real imagery, soap/candle physical proofs, legal/accounting review or a paid fulfilment. Keep those gates Failed/Blocked/In Progress until their detailed test and evidence requirements succeed.
