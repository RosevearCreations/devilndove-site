# Build 338 — Accounting Fixed Assets Read Extraction

Fixed-assets GET no longer creates `accounting_fixed_assets`. The explicit POST create path retains its existing write-side table ensure and insert behavior.
