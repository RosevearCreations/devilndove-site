# API Notes

Current sync: 2026-05-10 cleanup pass.

`/functions/api/` is the active Cloudflare Pages Functions backend. Duplicate root-level API files were removed from the active build.

Do not add API handlers outside this directory.
