// File: /functions/api/admin/site-item-inventory.js
// Brief description: Manages reorder and on-hand inventory for site-linked tools, supplies,
// products, and Amazon-linked items with deeper operational fields and editable admin actions.

function json(data, status = 200) { return new Response(JSON.stringify(data), { status, headers: { "Content-Type": "application/json" } }); }
function normalizeText(value) { return String(value || '').trim(); }
function getBearerToken(request) { const authHeader = request.headers.get("Authorization") || ""; const match = authHeader.match(/^Bearer\s+(.+)$/i); return match ? String(match[1] || '').trim() : ''; }
function normalizeResults(result) { return Array.isArray(result?.results) ? result.results : []; }
async function getAdminUserFromRequest(request, env) { const token = getBearerToken(request); if (!token) return null; const session = await env.DB.prepare(`SELECT s.user_id, u.user_id AS resolved_user_id, u.email, u.display_name, u.role, u.is_active FROM sessions s INNER JOIN users u ON u.user_id = s.user_id WHERE (s.session_token = ? OR s.token = ?) AND s.expires_at > datetime('now') LIMIT 1`).bind(token, token).first(); if (!session || Number(session.is_active || 0) !== 1 || String(session.role || '').toLowerCase() !== 'admin') return null; return { user_id: Number(session.resolved_user_id || session.user_id || 0), email: session.email || '', display_name: session.display_name || '', role: 'admin' }; }
function shape(row) {
  return {
    site_item_inventory_id: Number(row.site_item_inventory_id || 0),
    source_type: row.source_type || '',
    external_key: row.external_key || '',
    item_name: row.item_name || '',
    category: row.category || '',
    source_url: row.source_url || '',
    amazon_url: row.amazon_url || '',
    on_hand_quantity: Number(row.on_hand_quantity || 0),
    reserved_quantity: Number(row.reserved_quantity || 0),
    incoming_quantity: Number(row.incoming_quantity || 0),
    reorder_level: Number(row.reorder_level || 0),
    unit_cost_cents: Number(row.unit_cost_cents || 0),
    supplier_name: row.supplier_name || '',
    supplier_sku: row.supplier_sku || '',
    reorder_notes: row.reorder_notes || '',
    is_active: Number(row.is_active || 0),
    last_seen_at: row.last_seen_at || null,
    updated_at: row.updated_at || null,
    available_quantity: Number(row.on_hand_quantity || 0) - Number(row.reserved_quantity || 0),
    projected_quantity: Number(row.on_hand_quantity || 0) - Number(row.reserved_quantity || 0) + Number(row.incoming_quantity || 0),
    needs_reorder: (Number(row.on_hand_quantity || 0) - Number(row.reserved_quantity || 0) + Number(row.incoming_quantity || 0)) <= Number(row.reorder_level || 0)
  };
}
export async function onRequestGet(context) { const { request, env } = context; const adminUser = await getAdminUserFromRequest(request, env); if (!adminUser) return json({ ok: false, error: 'Unauthorized.' }, 401); const url = new URL(request.url); const source_type = normalizeText(url.searchParams.get('source_type')); const q = normalizeText(url.searchParams.get('q')).toLowerCase(); const rows = normalizeResults(await env.DB.prepare(`SELECT site_item_inventory_id, source_type, external_key, item_name, category, source_url, amazon_url, on_hand_quantity, reserved_quantity, incoming_quantity, reorder_level, unit_cost_cents, supplier_name, supplier_sku, reorder_notes, is_active, last_seen_at, updated_at FROM site_item_inventory WHERE (? = '' OR source_type = ?) AND (? = '' OR LOWER(COALESCE(item_name,'')) LIKE ? OR LOWER(COALESCE(category,'')) LIKE ? OR LOWER(COALESCE(supplier_name,'')) LIKE ?) ORDER BY CASE WHEN COALESCE(is_active,0)=1 THEN 0 ELSE 1 END, CASE WHEN (COALESCE(on_hand_quantity,0) - COALESCE(reserved_quantity,0) + COALESCE(incoming_quantity,0)) <= COALESCE(reorder_level,0) THEN 0 ELSE 1 END, source_type ASC, category ASC, item_name ASC`).bind(source_type, source_type, q, `%${q}%`, `%${q}%`, `%${q}%`).all()); const shaped = rows.map(shape); return json({ ok: true, items: shaped, summary: { total_items: shaped.length, active_items: shaped.filter((x) => x.is_active === 1).length, low_stock_items: shaped.filter((x) => x.needs_reorder).length, total_on_hand: shaped.reduce((sum, x) => sum + x.on_hand_quantity, 0), total_reserved: shaped.reduce((sum, x) => sum + x.reserved_quantity, 0), total_incoming: shaped.reduce((sum, x) => sum + x.incoming_quantity, 0) } }); }
export async function onRequestPost(context) { const { request, env } = context; const adminUser = await getAdminUserFromRequest(request, env); if (!adminUser) return json({ ok: false, error: 'Unauthorized.' }, 401); let body = {}; try { body = await request.json(); } catch { return json({ ok: false, error: 'Invalid JSON body.' }, 400); } const source_type = normalizeText(body.source_type) || 'supply'; const external_key = normalizeText(body.external_key) || crypto.randomUUID(); const item_name = normalizeText(body.item_name); if (!item_name) return json({ ok: false, error: 'item_name is required.' }, 400); await env.DB.prepare(`INSERT INTO site_item_inventory (source_type, external_key, item_name, category, source_url, amazon_url, on_hand_quantity, reserved_quantity, incoming_quantity, reorder_level, unit_cost_cents, supplier_name, supplier_sku, reorder_notes, is_active, last_seen_at, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP) ON CONFLICT(source_type, external_key) DO UPDATE SET item_name = excluded.item_name, category = excluded.category, source_url = excluded.source_url, amazon_url = excluded.amazon_url, on_hand_quantity = excluded.on_hand_quantity, reserved_quantity = excluded.reserved_quantity, incoming_quantity = excluded.incoming_quantity, reorder_level = excluded.reorder_level, unit_cost_cents = excluded.unit_cost_cents, supplier_name = excluded.supplier_name, supplier_sku = excluded.supplier_sku, reorder_notes = excluded.reorder_notes, is_active = excluded.is_active, last_seen_at = CURRENT_TIMESTAMP, updated_at = CURRENT_TIMESTAMP`).bind(source_type, external_key, item_name, normalizeText(body.category) || null, normalizeText(body.source_url) || null, normalizeText(body.amazon_url) || null, Number(body.on_hand_quantity || 0), Number(body.reserved_quantity || 0), Number(body.incoming_quantity || 0), Number(body.reorder_level || 0), Number(body.unit_cost_cents || 0), normalizeText(body.supplier_name) || null, normalizeText(body.supplier_sku) || null, normalizeText(body.reorder_notes) || null, Number(body.is_active) === 0 ? 0 : 1).run(); return json({ ok: true, message: 'Site inventory item saved.', source_type, external_key }); }
export async function onRequestPatch(context) { const { request, env } = context; const adminUser = await getAdminUserFromRequest(request, env); if (!adminUser) return json({ ok: false, error: 'Unauthorized.' }, 401); let body = {}; try { body = await request.json(); } catch { return json({ ok: false, error: 'Invalid JSON body.' }, 400); } const id = Number(body.site_item_inventory_id || 0); if (!Number.isInteger(id) || id <= 0) return json({ ok: false, error: 'A valid site_item_inventory_id is required.' }, 400); const existing = await env.DB.prepare(`SELECT * FROM site_item_inventory WHERE site_item_inventory_id = ? LIMIT 1`).bind(id).first(); if (!existing) return json({ ok: false, error: 'Site inventory item not found.' }, 404); await env.DB.prepare(`UPDATE site_item_inventory SET item_name = ?, category = ?, source_url = ?, amazon_url = ?, on_hand_quantity = ?, reserved_quantity = ?, incoming_quantity = ?, reorder_level = ?, unit_cost_cents = ?, supplier_name = ?, supplier_sku = ?, reorder_notes = ?, is_active = ?, updated_at = CURRENT_TIMESTAMP WHERE site_item_inventory_id = ?`).bind(normalizeText(body.item_name) || existing.item_name || null, body.category === undefined ? existing.category : (normalizeText(body.category) || null), body.source_url === undefined ? existing.source_url : (normalizeText(body.source_url) || null), body.amazon_url === undefined ? existing.amazon_url : (normalizeText(body.amazon_url) || null), body.on_hand_quantity === undefined ? Number(existing.on_hand_quantity || 0) : Number(body.on_hand_quantity || 0), body.reserved_quantity === undefined ? Number(existing.reserved_quantity || 0) : Number(body.reserved_quantity || 0), body.incoming_quantity === undefined ? Number(existing.incoming_quantity || 0) : Number(body.incoming_quantity || 0), body.reorder_level === undefined ? Number(existing.reorder_level || 0) : Number(body.reorder_level || 0), body.unit_cost_cents === undefined ? Number(existing.unit_cost_cents || 0) : Number(body.unit_cost_cents || 0), body.supplier_name === undefined ? existing.supplier_name : (normalizeText(body.supplier_name) || null), body.supplier_sku === undefined ? existing.supplier_sku : (normalizeText(body.supplier_sku) || null), body.reorder_notes === undefined ? existing.reorder_notes : (normalizeText(body.reorder_notes) || null), body.is_active === undefined ? Number(existing.is_active || 0) : (Number(body.is_active) === 0 ? 0 : 1), id).run(); return json({ ok: true, message: 'Site inventory item updated.', site_item_inventory_id: id }); }
export async function onRequestDelete(context) { const { request, env } = context; const adminUser = await getAdminUserFromRequest(request, env); if (!adminUser) return json({ ok: false, error: 'Unauthorized.' }, 401); const url = new URL(request.url); const id = Number(url.searchParams.get('site_item_inventory_id') || 0); if (!Number.isInteger(id) || id <= 0) return json({ ok: false, error: 'A valid site_item_inventory_id is required.' }, 400); await env.DB.prepare(`DELETE FROM site_item_inventory WHERE site_item_inventory_id = ?`).bind(id).run(); return json({ ok: true, message: 'Site inventory item deleted.', site_item_inventory_id: id }); }
