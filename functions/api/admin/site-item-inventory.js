import { auditAdminAction, getAdminUserFromRequest, getDb, jsonResponse, normalizeText } from "../_lib/adminAudit.js";
export async function onRequestPatch(context) { const { request, env } = context; const db=getDb(env); const adminUser=await getAdminUserFromRequest(request, env); if(!adminUser) return json({ok:false,error:'Unauthorized.'},401); let body={}; try{ body=await request.json(); }catch{ return json({ok:false,error:'Invalid JSON body.'},400); } const id=Number(body.site_item_inventory_id||0); if(!id) return json({ok:false,error:'site_item_inventory_id is required.'},400); try{ const existing=await db.prepare(`SELECT * FROM site_item_inventory WHERE site_item_inventory_id = ? LIMIT 1`).bind(id).first(); if(!existing) return json({ok:false,error:'Inventory item not found.'},404); const merged={ ...existing, ...body, item_name: normalizeText(body.item_name || existing.item_name), category: normalizeText(body.category ?? existing.category), source_url: normalizeText(body.source_url ?? existing.source_url), amazon_url: normalizeText(body.amazon_url ?? existing.amazon_url), image_url: normalizeText(body.image_url ?? existing.image_url), supplier_name: normalizeText(body.supplier_name ?? existing.supplier_name), supplier_sku: normalizeText(body.supplier_sku ?? existing.supplier_sku), reorder_notes: normalizeText(body.reorder_notes ?? existing.reorder_notes), reuse_status: normalizeText(body.reuse_status ?? existing.reuse_status) }; await db.prepare(`UPDATE site_item_inventory SET item_name=?, category=?, source_url=?, amazon_url=?, image_url=?, on_hand_quantity=?, reserved_quantity=?, incoming_quantity=?, reorder_level=?, unit_cost_cents=?, supplier_name=?, supplier_sku=?, reorder_notes=?, is_active=?, preferred_reorder_quantity=?, is_on_reorder_list=?, do_not_reorder=?, do_not_reuse=?, reuse_status=?, updated_at=CURRENT_TIMESTAMP WHERE site_item_inventory_id=?`).bind(merged.item_name, merged.category || null, merged.source_url || null, merged.amazon_url || null, merged.image_url || null, Number(merged.on_hand_quantity||0), Number(merged.reserved_quantity||0), Number(merged.incoming_quantity||0), Number(merged.reorder_level||0), Number(merged.unit_cost_cents||0), merged.supplier_name || null, merged.supplier_sku || null, merged.reorder_notes || null, Number(merged.is_active)===0 ? 0 : 1, Number(merged.preferred_reorder_quantity||0), Number(merged.is_on_reorder_list)===1 ? 1 : 0, Number(merged.do_not_reorder)===1 ? 1 : 0, Number(merged.do_not_reuse)===1 ? 1 : 0, merged.reuse_status || null, id).run(); await logMovement(db,{ site_item_inventory_id:id, source_type:existing.source_type, external_key:existing.external_key, item_name:merged.item_name, movement_type:'update', quantity_delta:Number(merged.on_hand_quantity||0)-Number(existing.on_hand_quantity||0), previous_on_hand_quantity:Number(existing.on_hand_quantity||0), new_on_hand_quantity:Number(merged.on_hand_quantity||0), previous_reserved_quantity:Number(existing.reserved_quantity||0), new_reserved_quantity:Number(merged.reserved_quantity||0), previous_incoming_quantity:Number(existing.incoming_quantity||0), new_incoming_quantity:Number(merged.incoming_quantity||0), note:normalizeText(body.movement_note)||'Inventory item updated.', actor_user_id:adminUser.user_id }); const saved=await db.prepare(`SELECT * FROM site_item_inventory WHERE site_item_inventory_id = ? LIMIT 1`).bind(id).first(); await auditAdminAction(env, request, adminUser, { action_type: "inventory_update", target_type: "inventory_item", target_id: id, target_key: `${existing.source_type}:${existing.external_key}`, details: { item_name: merged.item_name, previous_on_hand_quantity: Number(existing.on_hand_quantity||0), new_on_hand_quantity: Number(merged.on_hand_quantity||0), previous_reserved_quantity: Number(existing.reserved_quantity||0), new_reserved_quantity: Number(merged.reserved_quantity||0), previous_incoming_quantity: Number(existing.incoming_quantity||0), new_incoming_quantity: Number(merged.incoming_quantity||0) } }); return json({ok:true,item:shape(saved||{})}); }catch(e){ return json({ok:false,error:e.message||'Failed to update inventory item.'},500); } }
export async function onRequestDelete(context) { const { request, env } = context; const db=getDb(env); const adminUser=await getAdminUserFromRequest(request, env); if(!adminUser) return json({ok:false,error:'Unauthorized.'},401); const id=Number(new URL(request.url).searchParams.get('site_item_inventory_id')||0); if(!id) return json({ok:false,error:'site_item_inventory_id is required.'},400); try{ const existing=await db.prepare(`SELECT * FROM site_item_inventory WHERE site_item_inventory_id = ? LIMIT 1`).bind(id).first(); if(!existing) return json({ok:false,error:'Inventory item not found.'},404); await db.prepare(`DELETE FROM site_item_inventory WHERE site_item_inventory_id = ?`).bind(id).run(); await logMovement(db,{ site_item_inventory_id:id, source_type:existing.source_type, external_key:existing.external_key, item_name:existing.item_name, movement_type:'delete', quantity_delta:0, previous_on_hand_quantity:Number(existing.on_hand_quantity||0), new_on_hand_quantity:0, previous_reserved_quantity:Number(existing.reserved_quantity||0), new_reserved_quantity:0, previous_incoming_quantity:Number(existing.incoming_quantity||0), new_incoming_quantity:0, note:'Inventory item deleted.', actor_user_id:adminUser.user_id }); await auditAdminAction(env, request, adminUser, { action_type: "inventory_delete", target_type: "inventory_item", target_id: id, target_key: `${existing.source_type}:${existing.external_key}`, details: { item_name: existing.item_name, on_hand_quantity: Number(existing.on_hand_quantity||0) } }); return json({ok:true,message:'Inventory item removed.'}); }catch(e){ return json({ok:false,error:e.message||'Failed to remove inventory item.'},500); } }