// File: /functions/api/admin/product-qa-history.js
// Brief description: Reads persisted product QA history for catalog badges/panels.
import { getAdminUserFromRequest, getDb, jsonResponse } from '../_lib/adminAudit.js';
function json(data,status=200){return jsonResponse(data,status)} function rows(r){return Array.isArray(r?.results)?r.results:[]}
async function ensure(db){await db.prepare(`CREATE TABLE IF NOT EXISTS product_publish_qa_results (product_publish_qa_result_id INTEGER PRIMARY KEY AUTOINCREMENT, product_id INTEGER NOT NULL, qa_status TEXT NOT NULL DEFAULT 'unchecked', qa_score INTEGER NOT NULL DEFAULT 0, qa_summary_json TEXT, created_by_user_id INTEGER, created_at TEXT DEFAULT CURRENT_TIMESTAMP)`).run()}
export async function onRequestGet(context){const db=getDb(context.env); if(!db)return json({ok:false,error:'Database binding is missing.'},500); const user=await getAdminUserFromRequest(context.request,context.env); if(!user)return json({ok:false,error:'Unauthorized.'},401); await ensure(db); const url=new URL(context.request.url); const id=Number(url.searchParams.get('product_id')||0); const history=rows(await db.prepare(`SELECT * FROM product_publish_qa_results WHERE (?<=0 OR product_id=?) ORDER BY datetime(created_at) DESC LIMIT 200`).bind(id,id).all().catch(()=>({results:[]}))); return json({ok:true,history,summary:{total:history.length,latest:history[0]||null}})}
