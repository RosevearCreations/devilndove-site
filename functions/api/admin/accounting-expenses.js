import { getAdminUserFromRequest, getDb, jsonResponse, auditAdminAction, normalizeText } from "../_lib/adminAudit.js";
function nr(r){ return Array.isArray(r?.results)?r.results:[]; }
async function ensureTable(db){ await db.prepare(`CREATE TABLE IF NOT EXISTS accounting_expenses ( expense_id INTEGER PRIMARY KEY AUTOINCREMENT, expense_date TEXT, vendor_name TEXT, amount REAL NOT NULL DEFAULT 0, tax_amount REAL NOT NULL DEFAULT 0, ledger_code TEXT, ledger_name TEXT, notes TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP )`).run(); }
async function lookupLedgerName(db, code){ if(!code) return ''; try{ const row=await db.prepare(`SELECT name FROM general_ledger_accounts WHERE code=? LIMIT 1`).bind(code).first(); return row?.name||''; }catch{return '';} }
export async function onRequestGet(context){ const adminUser=await getAdminUserFromRequest(context.request, context.env); if(!adminUser) return jsonResponse({ok:false,error:"Admin access required."},401); const db=getDb(context.env); if(!db) return jsonResponse({ok:false,error:"Database binding is not configured."},500); await ensureTable(db); const result=await db.prepare(`SELECT expense_id, expense_date, vendor_name, amount, tax_amount, ledger_code, ledger_name, notes, created_at, updated_at FROM accounting_expenses ORDER BY COALESCE(expense_date, created_at) DESC, expense_id DESC`).all(); return jsonResponse({ok:true, expenses:nr(result)}); }
export async function onRequestPost(context){ const adminUser=await getAdminUserFromRequest(context.request, context.env); if(!adminUser) return jsonResponse({ok:false,error:"Admin access required."},401); const db=getDb(context.env); if(!db) return jsonResponse({ok:false,error:"Database binding is not configured."},500); await ensureTable(db); let body={}; try{body=await context.request.json();}catch{} const expense_date=normalizeText(body.expense_date); const vendor_name=normalizeText(body.vendor_name); const amount=Number(body.amount||0); const tax_amount=Number(body.tax_amount||0); const ledger_code=normalizeText(body.ledger_code).toUpperCase(); const ledger_name=await lookupLedgerName(db, ledger_code); const notes=normalizeText(body.notes); if(!vendor_name || !Number.isFinite(amount)) return jsonResponse({ok:false,error:"Vendor and amount are required."},400); await db.prepare(`INSERT INTO accounting_expenses (expense_date,vendor_name,amount,tax_amount,ledger_code,ledger_name,notes,updated_at) VALUES (?,?,?,?,?,?,?,CURRENT_TIMESTAMP)`).bind(expense_date||null,vendor_name,amount,tax_amount,ledger_code||null,ledger_name||null,notes||null).run(); await auditAdminAction(context.env, context.request, adminUser, { action_type:"create_expense", target_type:"accounting_expense", details:{ expense_date,vendor_name,amount,ledger_code } }); return jsonResponse({ok:true}); }