// File: /functions/api/admin/dark-theme-evidence.js
// Brief description: Store dark-theme screenshot/evidence rows for public-page visual regression reviews.
import { getAdminUserFromRequest, getDb, jsonResponse, normalizeText } from '../_lib/adminAudit.js';
function json(d,s=200){return jsonResponse(d,s,{ 'Cache-Control':'no-store' });} function rows(r){return Array.isArray(r?.results)?r.results:[];} function clean(v,l=1200){const t=normalizeText(v);return t.length>l?t.slice(0,l).trim():t;}
async function ensure(db){await db.prepare(`CREATE TABLE IF NOT EXISTS dark_theme_screenshot_evidence (dark_theme_screenshot_evidence_id INTEGER PRIMARY KEY AUTOINCREMENT,page_path TEXT NOT NULL,evidence_url TEXT,section_label TEXT,review_status TEXT NOT NULL DEFAULT 'needs_review',contrast_status TEXT NOT NULL DEFAULT 'unchecked',notes TEXT,created_by_user_id INTEGER,created_at TEXT DEFAULT CURRENT_TIMESTAMP,updated_at TEXT DEFAULT CURRENT_TIMESTAMP)`).run()}
export async function onRequestGet(context){const db=getDb(context.env);if(!db)return json({ok:false,error:'Database binding is missing.'},500);const user=await getAdminUserFromRequest(context.request,context.env);if(!user)return json({ok:false,error:'Unauthorized.'},401);await ensure(db);const url=new URL(context.request.url);const page=clean(url.searchParams.get('page_path')||'',400);const items=rows(await db.prepare(`SELECT * FROM dark_theme_screenshot_evidence WHERE (?='' OR page_path=?) ORDER BY datetime(updated_at) DESC LIMIT 200`).bind(page,page).all().catch(()=>({results:[]})));return json({ok:true,items,summary:{total:items.length,needs_review:items.filter(i=>i.review_status==='needs_review').length,failed:items.filter(i=>i.contrast_status==='failed').length}})}
export async function onRequestPost(context){const db=getDb(context.env);if(!db)return json({ok:false,error:'Database binding is missing.'},500);const user=await getAdminUserFromRequest(context.request,context.env);if(!user)return json({ok:false,error:'Unauthorized.'},401);await ensure(db);let body={};try{body=await context.request.json()}catch{return json({ok:false,error:'Invalid JSON body.'},400)}const page=clean(body.page_path||'',400);if(!page)return json({ok:false,error:'page_path is required.'},400);await db.prepare(`INSERT INTO dark_theme_screenshot_evidence (page_path,evidence_url,section_label,review_status,contrast_status,notes,created_by_user_id,created_at,updated_at) VALUES (?,?,?,?,?,?,?,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP)`).bind(page,clean(body.evidence_url||'',1200),clean(body.section_label||'',200),clean(body.review_status||'needs_review',80),clean(body.contrast_status||'unchecked',80),clean(body.notes||'',1200),Number(user.user_id||0)||null).run();return json({ok:true,message:'Dark-theme screenshot evidence stored.'})}
