# Devil n Dove — Project Status and Roadmap

## Current Development and Production authority

**Release 467 Build 93 — Centered Application Shell & Overflow Accessibility** is the current Development closure candidate.

Last fully verified Development is Build 92 — Prelaunch Action Queue Completeness & Ownership:
- `dev` `67bca9198c0973ffe2b39818c3b933ec2737cc00`
- tree `f3fa062060cd53eb5e4b7dab42b2ba6d1fae0450`
- System Gate `34506095955` SUCCESS
- Current Application Quality `34506095848` SUCCESS
- I.T. Admin Runtime Proof `34506095837` SUCCESS
- Repository Branch Hygiene `34506095835` SUCCESS
- exact Preview, canonical Development D1, read-only data authority, bindings, non-secret smoke and regression evidence: SUCCESS.

Current Production is also Build 92:
- `main` `67bca9198c0973ffe2b39818c3b933ec2737cc00`
- tree `f3fa062060cd53eb5e4b7dab42b2ba6d1fae0450`
- Production Pages Deploy `34506354596` SUCCESS
- Production Live Resource Integrity `34506453451` SUCCESS.

## Build 93 scope

Build 93 fixes the shared off-center/unreachable-right-side layout defect across the application rather than patching one page. The responsive convergence layer now centers public and admin shells, removes root horizontal clipping, bounds grid/card children and long tokens to the viewport, and keeps wide data regions locally horizontally scrollable. The dynamic table guard also makes those scroll regions keyboard focusable and labeled for accessibility.

This is a presentation/runtime-layout change only. It does not modify D1/R2 business data, schema, payment/provider execution, publication authority, commerce rules or heading hierarchy. Exactly one H1 remains the public SEO rule.

## Current external acceptance

Stripe Development, PayPal sandbox, Social OAuth and Cloudflare Access remain `HOLD_EXTERNAL`. CAIP private-media acceptance remains `EVIDENCE_DEPENDENT`. Canada-only CA/CAD commerce remains active, U.S. sales/shipping remain disabled, and local pickup remains supported.

## Build 93 closure sequence

1. Ingest Build 92 exact Development/Production closure into current restart authority.
2. Apply centered-shell and horizontal-reachability safeguards globally and chain the Build 93 source contract.
3. Fast-forward the exact candidate to `dev`.
4. Require exact merged-`dev` System Gate, Current Application Quality, I.T. Admin Runtime Proof and Repository Branch Hygiene plus canonical Development D1/binding proof and exact Preview smoke.
5. Promote the same exact SHA/tree to `main` only when all Development checks are GREEN.
6. Require Production business-data preservation, canonical Production D1/FK/binding proof, exact Pages deployment, public smoke and promotion proof.
7. Require Production Live Resource Integrity to re-prove live D1, same-origin Product R2, Product API photography and account diagnostic.
8. Only then call Build 93 complete externally; the following build must ingest Build 93's final external closure evidence.

Canonical migrations remain exactly `0001`–`0004`.
