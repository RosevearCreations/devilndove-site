#!/usr/bin/env python3
"""Canonical Release 465 + Release 463 environment forward sanity."""
from __future__ import annotations
import json
from pathlib import Path
ROOT=Path(__file__).resolve().parents[1];FAIL=[];LABEL='Business Intelligence and Release Hardening';ALLOWED={'release465_build3_source_candidate','release465_complete_development_green'}
MIGRATIONS=['migrations/canonical/0001_release464_migration_authority.sql','migrations/canonical/0002_release464_operational_acceptance.sql','migrations/canonical/0003_release464_business_growth.sql','migrations/canonical/0004_release465_storefront_quality.sql']
def read(p):
 f=ROOT/p
 if not f.is_file():FAIL.append(f'missing file: {p}');return''
 return f.read_text(encoding='utf-8',errors='replace')
def req(ok,msg):
 if not ok:FAIL.append(msg)
r=json.loads(read('development-release.json') or '{}');env=json.loads(read('release463-environment.json') or '{}');manifest=json.loads(read('migrations/canonical/manifest.json') or '{}');b1=json.loads(read('release465-build1-storefront-quality.json') or '{}');b2=json.loads(read('release465-build2-inventory-creator-intelligence.json') or '{}');b3=json.loads(read('release465-build3-financial-it-hardening.json') or '{}');state=r.get('convergence_state')
req(r.get('environment')=='development' and r.get('branch')=='dev','application authority must remain Development/dev');req(int(r.get('release') or 0)==465 and r.get('label')==LABEL,'current Release 465 authority drifted');req(state in ALLOWED,'Build 3 convergence state unsupported');req(int(r.get('environment_release') or 0)==463,'environment overlay must remain Release 463');req([x.get('key') for x in r.get('canonical_modules',[])]==['storefront','creators','socials','financials','it-platform'],'five-module authority drifted')
dev=r.get('development_infrastructure') or {};d1=dev.get('d1') or {};prod=r.get('production_infrastructure') or {};pd1=prod.get('d1') or {};req(dev.get('pages_project')=='devilndove-site' and dev.get('pages_environment')=='preview' and dev.get('pages_branch')=='dev','Development Pages boundary drifted');req(d1.get('database_name')=='devilndove-dev' and d1.get('database_id')=='dbc1615b-dcbe-4951-973b-b47c99c73bfa','Development D1 drifted');req(prod.get('branch')=='main' and pd1.get('database_name')=='devilndove-prod-r462' and pd1.get('database_id')=='f34a741b-0000-45b0-9a96-6be08754d563','Production authority drifted');req(d1.get('database_id')!=pd1.get('database_id'),'Development and Production D1 must remain isolated')
p=r.get('release_policy') or {};req(p.get('historical_migration_replay') is False,'historical replay must remain false');req(p.get('production_promotion')=='exact_green_development_tree_only' and p.get('main_only_application_patches') is False,'promotion boundary drifted');req(p.get('future_d1_schema_changes')=='migrations/canonical_only' and p.get('development_first_schema_verification') is True and p.get('production_migration_before_dependent_code') is True,'migration order/authority drifted');req(p.get('production_transactional_data_owned_by_production') is True and p.get('blind_dev_to_production_data_overwrite') is False,'Production data ownership drifted');req(p.get('provider_execution')=='closed' and p.get('provider_publication')=='closed' and p.get('provider_live_authorization')=='closed','provider boundary drifted');req(p.get('request_time_schema_mutation')=='blocked_by_runtime_firewall_and_source_gate' and p.get('preview_access_must_not_be_weakened_for_smoke') is True,'runtime/Access safety drifted')
rm=r.get('current_release_migrations') or [];req([x.get('file') for x in rm]==MIGRATIONS,'current migration list must remain exact 0001-0004');req(all(x.get('development_apply')=='applied_and_verified' for x in rm),'Development migrations must remain proven');req(all(x.get('production_apply')=='production_promotion_gate_managed_not_yet_promoted' for x in rm),'Production migrations must remain unpromoted');req([x.get('file') for x in manifest.get('migrations',[])]==[Path(x).name for x in MIGRATIONS],'manifest sequence drifted')
db=r.get('current_release_database_state') or {};req(db.get('new_development_migration_required') is False,'Build 3 must not request a migration');req(db.get('build2_schema_change_required') is False and db.get('build2_migration')=='none','Build 2 schema boundary drifted');req(db.get('build3_schema_change_required') is False and db.get('build3_migration')=='none','Build 3 must remain schema-neutral');req(int(db.get('development_native_migration_rows') or 0)==4 and int(db.get('development_migration_proof_rows') or 0)==4,'Development migration proof must remain 4/4')
req(b1.get('state')=='complete_development_green' and all(x.get('status')=='complete_development_green' for x in b1.get('items',[])),'Build 1 must remain green');req(b2.get('state')=='complete_development_green' and all(x.get('status')=='complete_development_green' for x in b2.get('items',[])),'Build 2 must remain green');req([x.get('id') for x in b3.get('items',[])]==[14,15,16,17,18,19,20],'Build 3 authority item identity drifted')
r3=r.get('release465_build3') or [];req([x.get('id') for x in r3]==[14,15,16,17,18,19,20],'Build 3 release identity drifted');planned={int(x.get('build') or 0):x for x in r.get('planned_builds',[])}
if state=='release465_build3_source_candidate':
 req(b3.get('state')=='in_progress_source_candidate' and all(x.get('status')=='implemented_source_candidate' for x in r3),'Build 3 candidate state drifted');req(planned.get(3,{}).get('items')==[14,15,16,17,18,19,20],'Build 3 must remain in-progress')
else:
 req(b3.get('state')=='complete_development_green' and all(x.get('status')=='complete_development_green' for x in r3),'Build 3 closure state drifted');req(3 not in planned,'Completed Build 3 must not remain planned');e=r.get('current_release_evidence') or {};req(e.get('build3_development_green') is True,'Build 3 green evidence missing');req(int(e.get('development_native_migration_rows') or 0)==4 and int(e.get('development_migration_proof_rows') or 0)==4 and int(e.get('development_foreign_key_violations',-1))==0,'Build 3 D1 closure evidence drifted');req(e.get('preview_smoke_pass') is True and e.get('preview_smoke_mode')=='CLOUDFLARE_ACCESS_PROTECTED','Build 3 smoke evidence missing')
req(int(env.get('environment_release') or 0)==463 and env.get('canonical_pages_project')=='devilndove-site','Release 463 environment authority drifted');req((env.get('native_git_deployments') or {}).get('enabled') is False,'native Git-triggered Pages deploy must remain frozen')
wr=read('wrangler.toml');req('name = "devilndove-site"' in wr and 'migrations_dir = "migrations/canonical"' in wr and 'database_id = "dbc1615b-dcbe-4951-973b-b47c99c73bfa"' in wr,'tracked Wrangler Development authority drifted');req('f34a741b-0000-45b0-9a96-6be08754d563' not in wr and 'account_id =' not in wr,'tracked Wrangler contains forbidden Production/account identity')
for path in ('AI_HANDOFF.md','PROJECT_STATUS_AND_ROADMAP.md','SANITY_HEALTH_CHECK.md','docs/operations/RELEASE_465_THREE_BUILD_ROADMAP.md','release465-build1-storefront-quality.json','release465-build2-inventory-creator-intelligence.json','release465-build3-financial-it-hardening.json','release465-performance-budget.json','scripts/release465_build1_gate.py','scripts/release465_build2_gate.py','scripts/release465_build3_gate.py','scripts/release465_performance_budget_gate.py','scripts/release465_regression_evidence.py'):req((ROOT/path).is_file(),f'canonical authority missing: {path}')
print('PLATFORM FORWARD SANITY');print('Application release: 465 —',LABEL);print('Build 1: DEVELOPMENT GREEN');print('Build 2: DEVELOPMENT GREEN');print('Build 3:','SOURCE CANDIDATE' if state.endswith('source_candidate') else 'DEVELOPMENT GREEN');print('Canonical D1: 0001-0004 / Build 3 migration NONE');print('Production: CLOSED')
if FAIL:
 [print(f'{i:03d}. FAIL — {x}') for i,x in enumerate(FAIL,1)];raise SystemExit(1)
print('PLATFORM FORWARD SANITY: PASS')
