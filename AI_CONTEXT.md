# Devil n Dove AI Context — Development Build 293 Pointer

Read `AI_HANDOFF.md` for retained business/data safety history and `PROJECT_STATUS_AND_ROADMAP.md` for the pre-modular functional roadmap. For the modular Development line after the Build 280 Production freeze, read `docs/architecture/MODULAR_APPLICATION_ARCHITECTURE.md`, `docs/architecture/BUILD282_ARCHITECTURE_LOCK.md`, `docs/architecture/BUILD283_PACKAGING_MODULE_ACTIVATION.md`, `docs/architecture/BUILD284_PACKAGING_CONTRACT_INTEGRATION.md`, `docs/architecture/BUILD285_PACKAGING_CONTRACT_CONSUMPTION.md`, `docs/architecture/BUILD286_PACKAGING_API_BOUNDARY_CLEANUP.md`, `docs/architecture/BUILD287_PACKAGING_CONTENT_ARTWORK_PICKER.md`, `docs/architecture/BUILD288_PACKAGING_LEGACY_GET_RETIREMENT.md`, `docs/architecture/BUILD289_PACKAGING_WRITE_RESPONSE_DECOUPLING.md`, `docs/architecture/BUILD290_PACKAGING_LEGACY_BROAD_READ_SOURCE_REMOVAL.md`, `docs/architecture/BUILD291_PACKAGING_DOMAIN_WRITE_SERVICE_EXTRACTION.md`, `docs/architecture/BUILD292_PACKAGING_LEGACY_POST_RETIREMENT.md`, and `docs/architecture/BUILD293_PACKAGING_READ_SERVICE_EXTRACTION.md`.

**Production is frozen at Build 280 unless deliberately promoted through the separate Production workflow. Development has intentionally diverged.**

Builds 281–290 established the modular registry, Packaging activation, owner-side read contracts, narrow Packaging bootstrap, Content artwork selection, active-runtime legacy GET retirement, write-response decoupling, and physical removal of broad Catalog/Inventory reads from the mature Packaging server source.

Build 291 moved the exact mature Build 290 Packaging implementation into `functions/api/_lib/packagingDomainService.js`, and the native `/api/admin/packaging-write` endpoint began calling that shared write service directly. Build 292 then retired direct POST authority at `/api/admin/packaging-studio`; authenticated direct legacy POSTs now return HTTP 410 `packaging_legacy_post_retired` while the normal UI continues through the native write gateway.

Build 293 extracts the remaining active Packaging-owned GET/read implementation into `functions/api/_lib/packagingReadService.js`. The new file is byte-for-byte equal to the final Build 292 `functions/api/admin/packaging-bootstrap.js`, preserving the proven Build 286 list/detail queries, preflight calculations, schema fallbacks, scoped Product context and scoped Inventory component joins without rewriting them.

`functions/api/admin/packaging-bootstrap.js` is now a thin Build 293 adapter around the shared read service. It preserves the client-facing Build 286 payload and mode, and adds `read_service_build: 293`, `read_implementation_build: 286`, `read_authority: packaging-read-service`, and `shared_read_service: true` provenance. Bulk Catalog and Inventory collections remain excluded and continue to arrive through their owner contracts.

The temporary GET side of `/api/admin/packaging-studio` now uses the same shared read service while preserving its legacy `build: "277"` and legacy mode response surface. Its Build 292 POST retirement is unchanged and still returns authenticated HTTP 410. The active browser runtime still blocks legacy GET transport and uses `/api/admin/packaging-bootstrap`, so this legacy GET remains compatibility-only until server-side retirement.

The Build 291 `packagingDomainService.js` write implementation and the Build 292 `/api/admin/packaging-write` gateway are unchanged in Build 293. No browser Packaging runtime file changes. Build 293 introduces no D1 migration, SQL/schema change, Wrangler/binding change, R2 enumeration, or Production change.

Expected next work after Build 293 parity validation: retire the remaining server-side legacy GET authority at `/api/admin/packaging-studio` with authenticated HTTP 410 semantics, then evaluate deletion of `functions/api/admin/packaging-studio.js`. A later cleanup can remove now-dormant read helpers from the write-service source only after write-response/detail behavior has an independent proven replacement.
