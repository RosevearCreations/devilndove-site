// Release 466 Build 4 — I.T. Operations Dashboard client.
(()=>{'use strict';
const byId=(id)=>document.getElementById(id),esc=(v)=>String(v==null?'':v).replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),api=(url)=>window.DDAuth?.apiFetch?window.DDAuth.apiFetch(url,{cache:'no-store'}):fetch(url,{credentials:'same-origin',cache:'no-store'});
async function read(r){const d=await r.json().catch(()=>null);if(!r.ok||d?.ok===false)throw new Error(d?.error||`HTTP ${r.status}`);return d}function msg(v,error=false){const n=byId('itOpsMessage');if(n){n.textContent=v||'';n.classList.toggle('is-error',error);n.classList.toggle('is-success',Boolean(v&&!error))}}function yes(v){return v?'YES':'NO'}
function render(d){const m=d.migrations||{},i=d.runtime_incidents||{},b=d.business_growth||{},p=d.providers||{},r=d.resources||{},ret=d.retention||{},h=d.health||{};byId('itOpsDashboard').innerHTML=`
<div class="it-metrics"><div class="card"><span class="small">I.T. health</span><strong>${Number(h.score||0)}/100</strong><div class="small">${esc(String(h.status||'review').toUpperCase())} • foreign_key_violations ${Number(h.foreign_key_violations||0)}</div></div><div class="card"><span class="small">Application</span><strong>Release ${Number(d.release||0)}</strong><div class="small">${esc(d.label||'')}</div></div><div class="card"><span class="small">Environment authority</span><strong>Release ${Number(d.environment_release||0)}</strong><div class="small">${esc(d.environment)} • ${esc(r.branch)}</div></div><div class="card"><span class="small">Canonical migrations</span><strong>${Number(m.native_rows||0)} / ${Number(m.expected||0)}</strong><div class="small">proof rows ${Number(m.proof_rows||0)} • ${m.ready?'ready':'review'}</div></div><div class="card"><span class="small">Open incidents</span><strong>${Number(i.open_count||0)}</strong><div class="small">critical ${Number(i.open_critical||0)} • errors ${Number(i.open_error||0)}</div></div><div class="card"><span class="small">Open pipelines</span><strong>${Number(b.open_pipelines||0)}</strong><div class="small">${Number(b.complete_pipelines||0)} complete</div></div></div>
<div class="grid cols-2" style="gap:18px;margin-top:18px"><section class="card"><h2 style="margin-top:0">Environment resources</h2><p class="small"><strong>Pages:</strong> ${esc(r.pages_project)} / ${esc(r.pages_environment)}<br><strong>D1:</strong> ${esc(r.d1)}<br><strong>Product R2:</strong> ${esc(r.product_r2)}<br><strong>CAIP R2:</strong> ${esc(r.caip_r2)}</p><p class="small">Retention approvals pending: ${Number(ret.pending_approval||0)}</p></section><section class="card"><h2 style="margin-top:0">Provider boundary</h2><div class="it-provider"><span><strong>Stripe</strong><span class="small" style="display:block">configured ${yes(p.stripe?.configured)} • webhook ${yes(p.stripe?.webhook_configured)} • ${esc(p.stripe?.execution_code||'')}</span></span><strong>${p.stripe?.execution_authorized?'AUTHORIZED':'CLOSED'}</strong></div><div class="it-provider" style="margin-top:8px"><span><strong>PayPal</strong><span class="small" style="display:block">configured ${yes(p.paypal?.configured)} • webhook ${yes(p.paypal?.webhook_configured)} • ${esc(p.paypal?.execution_code||'')}</span></span><strong>${p.paypal?.execution_authorized?'AUTHORIZED':'CLOSED'}</strong></div><p class="small">Snapshot only. This dashboard never executes a provider. Build 4 acceptance remains separate from configuration.</p></section></div>
<section class="card" style="margin-top:18px"><h2 style="margin-top:0">I.T. health dimensions</h2><ul class="small">${(h.checks||[]).map(x=>`<li>${x.pass?'✓':'○'} ${esc(x.label)} (${Number(x.weight||0)} pts)</li>`).join('')}</ul></section>
<section class="card" style="margin-top:18px"><h2 style="margin-top:0">Control-centre links</h2><div class="it-links"><a class="btn primary" href="/admin/release-control/external-commercial-readiness/">External Acceptance</a><a class="btn" href="${esc(d.links?.business_health||'/admin/business-health/')}">Business Health</a><a class="btn" href="${esc(d.links?.runtime_incidents||'/admin/operations/')}">Runtime Incidents</a><a class="btn" href="${esc(d.links?.business_pipeline||'/admin/business-pipeline/')}">Business Pipeline</a><a class="btn" href="${esc(d.links?.storefront_merchandising||'/admin/storefront-merchandising/')}">Merchandising</a><a class="btn" href="${esc(d.links?.accounting||'/admin/accounting/')}">Month End</a><a class="btn" href="${esc(d.links?.integrations||'/admin/it-integrations/')}">Integrations</a></div></section>`;}
async function load(){try{msg('Loading I.T. operations snapshot…');const d=await read(await api('/api/admin/it-operations-dashboard'));render(d);msg(`Release ${d.release} / Environment ${d.environment_release} operations snapshot loaded.`)}catch(e){msg(e.message,true)}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',load,{once:true});else load();
})();
