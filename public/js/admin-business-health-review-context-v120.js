// Release 467 Build 120 — destination-side URL-only Business Health review context banner.
(()=>{'use strict';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function init(){const q=new URLSearchParams(location.search);if(q.get('business_health_source')!=='build120')return;const period=q.get('business_health_period')||'current',owner=q.get('business_health_owner')||'review',state=q.get('business_health_state')||'review',action=q.get('business_health_action')||'',persistent=Number(q.get('business_health_persistent')||0),newly=Number(q.get('business_health_newly_worsening')||0),hero=document.querySelector('.hero');if(!hero)return;const card=document.createElement('section');card.className='card';card.style.marginTop='18px';card.setAttribute('data-business-health-context-build','120');card.innerHTML=`<p class="eyebrow">Build 120 Business Health review context</p><h2 style="margin-top:0">Human review handoff</h2><p class="small"><strong>Period:</strong> ${esc(period)} • <strong>Owner:</strong> ${esc(owner)} • <strong>Priority:</strong> ${esc(state.replaceAll('_',' '))}</p><p class="small"><strong>Top existing action:</strong> ${esc(action||'Review current evidence')} • ${persistent} persistent worsening • ${newly} newly worsening.</p><p class="small">This banner is URL context only. It does not acknowledge, approve, resolve or execute anything.</p><a class="btn secondary" href="/admin/business-health/?period_month=${encodeURIComponent(period)}">Return to Business Health →</a>`;hero.insertAdjacentElement('afterend',card);const month=document.getElementById('monthEndPeriod');if(month&&/^\d{4}-\d{2}$/.test(period)){month.value=period;setTimeout(()=>document.getElementById('monthEndRefresh')?.click(),0);}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
