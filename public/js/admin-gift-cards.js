// File: /public/js/admin-gift-cards.js
// Brief description: Gift-card admin delivery templates, resend queue, and abuse review.
document.addEventListener('DOMContentLoaded',()=>{const mount=document.getElementById('giftCardAdminMount');if(!mount||!window.DDAuth)return;const esc=(v)=>String(v??'').replace(/[&<>"']/g,(ch)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));async function read(r){const d=await r.json().catch(()=>null);if(!r.ok||!d?.ok)throw new Error(d?.error||'Gift-card admin failed.');return d;}async function load(){mount.innerHTML='<p class="small">Loading gift-card admin…</p>';try{const [templates,abuse]=await Promise.all([read(await window.DDAuth.apiFetch('/api/admin/gift-card-delivery-templates')),read(await window.DDAuth.apiFetch('/api/admin/gift-card-abuse')).catch(()=>({attempts:[],summary:{}}))]);mount.innerHTML=`<div class="grid cols-2"><section class="card"><h2 style="margin-top:0">Delivery templates</h2>${(templates.templates||[]).map(t=>`<form class="gift-template-form" data-template="${esc(t.template_key)}"><label class="small">Template key<input name="template_key" value="${esc(t.template_key)}"></label><label class="small">Subject<input name="subject" value="${esc(t.subject||'')}"></label><label class="small">Body<textarea name="body" rows="6">${esc(t.body||'')}</textarea></label><button class="btn" type="submit">Save template</button></form>`).join('')}</section><section class="card"><h2 style="margin-top:0">Resend delivery</h2><label class="small">Gift card ID<input id="giftResendId" type="number"></label><label class="small">Recipient email<input id="giftResendEmail" type="email"></label><label class="small">Template<select id="giftResendTemplate">${(templates.templates||[]).map(t=>`<option value="${esc(t.template_key)}">${esc(t.template_key)}</option>`).join('')}</select></label><button class="btn" id="giftResendButton" type="button">Queue resend</button><h3>Abuse signals</h3><p class="small">High risk groups: ${esc(abuse.summary?.high_risk||0)}</p><div class="admin-table-wrap"><table><thead><tr><th>Email/code</th><th>Attempts</th><th>Severity</th></tr></thead><tbody>${(abuse.attempts||[]).slice(0,20).map(a=>`<tr><td>${esc(a.lookup_email||'')}<div class="small">${esc(a.code_suffix||'')}</div></td><td>${esc(a.attempt_count||0)}</td><td>${esc(a.severity_label||a.severity_score||'low')}</td></tr>`).join('')}</tbody></table></div></section></div>`;mount.querySelectorAll('.gift-template-form').forEach(f=>f.addEventListener('submit',saveTemplate));document.getElementById('giftResendButton')?.addEventListener('click',resend);}catch(e){mount.innerHTML=`<p class="small">${esc(e.message||'Gift-card admin failed.')}</p>`}}async function saveTemplate(e){e.preventDefault();const payload=Object.fromEntries(new FormData(e.currentTarget).entries());await read(await window.DDAuth.apiFetch('/api/admin/gift-card-delivery-templates',{method:'POST',body:JSON.stringify(payload)}));await load()}async function resend(){await read(await window.DDAuth.apiFetch('/api/admin/gift-card-delivery-templates',{method:'POST',body:JSON.stringify({action:'resend',gift_card_id:Number(document.getElementById('giftResendId')?.value||0),recipient_email:document.getElementById('giftResendEmail')?.value||'',template_key:document.getElementById('giftResendTemplate')?.value||'activation'})}));await load()}load();});
