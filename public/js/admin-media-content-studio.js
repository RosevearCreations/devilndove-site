// Devil n Dove Build 256 — Media & Content Management Studio admin UI.
(()=>{
  'use strict';
  if(!window.DDAuth)return;
  const state={media:[],slots:[],pages:[],selected:null,uses:[],inspected:[],r2Cursor:'',pagePath:'/'};
  const id=(v)=>document.getElementById(v);
  const text=(v)=>String(v??'').trim();
  const esc=(v)=>String(v??'').replace(/[&<>'"]/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const pathValue=()=>{let p=text(id('mediaPagePath')?.value)||'/';try{if(/^https?:\/\//i.test(p))p=new URL(p,location.origin).pathname;}catch{}if(!p.startsWith('/'))p='/'+p;if(p.length>1&&p.endsWith('/'))p=p.slice(0,-1);return p||'/';};
  function msg(message,type='info'){const el=id('mediaStudioMessage');if(!el)return;el.hidden=false;el.className=`card small media-studio-message ${type}`;el.textContent=message;}
  async function read(response,fallback='Request failed.'){
    const raw=await response.text().catch(()=>''),ct=text(response.headers.get('content-type')).toLowerCase();let data=null;
    if(raw&&(ct.includes('json')||/^\s*[\[{]/.test(raw))){try{data=JSON.parse(raw);}catch{}}
    if(response.ok&&data?.ok)return data;
    throw new Error(data?.error||data?.detail||`${fallback} (${response.status||'network'})`);
  }
  async function api(body){return read(await window.DDAuth.apiFetch('/api/admin/media-content-studio',{method:'POST',body:JSON.stringify(body)}),'Media Studio update failed.');}
  function currentQuery(){const params=new URLSearchParams({path:pathValue(),limit:'160'});const q=text(id('mediaSearch')?.value),type=text(id('mediaTypeFilter')?.value),assignment=text(id('mediaAssignmentFilter')?.value);if(q)params.set('q',q);if(type)params.set('media_type',type);if(assignment)params.set('assignment',assignment);if(state.selected?.media_asset_id)params.set('media_id',String(state.selected.media_asset_id));return params;}
  async function load(){state.pagePath=pathValue();id('mediaCurrentPageLabel').textContent=`Page: ${state.pagePath}`;try{const data=await read(await window.DDAuth.apiFetch(`/api/admin/media-content-studio?${currentQuery()}`),'Media Studio could not load.');state.media=data.media||[];state.slots=data.slots||[];state.pages=data.pages||[];state.uses=data.media_uses||[];if(state.selected){const fresh=state.media.find(x=>Number(x.media_asset_id)===Number(state.selected.media_asset_id));if(fresh)state.selected=fresh;}renderAll();}catch(e){msg(e.message,'error');}}
  function fmtBytes(v){const n=Number(v||0);if(!n)return 'size unknown';if(n<1024)return `${n} B`;if(n<1048576)return `${(n/1024).toFixed(1)} KB`;return `${(n/1048576).toFixed(1)} MB`;}
  function renderLibrary(){const el=id('mediaLibraryGrid');if(!state.media.length){el.innerHTML='<p class="small">No media matched this filter. Upload a public image or use explicit R2 sync.</p>';return;}el.innerHTML=state.media.map((m)=>`<button type="button" class="media-thumb ${state.selected?.media_asset_id===m.media_asset_id?'selected':''}" data-media-id="${m.media_asset_id}"><span class="media-thumb-img">${m.public_url?`<img loading="lazy" src="${esc(m.public_url)}" alt=""/>`:'<span class="media-no-preview">No preview</span>'}</span><span class="media-thumb-copy"><strong>${esc(m.display_name||m.original_filename||m.object_key)}</strong><small>${esc(m.media_type||'photo')} • ${Number(m.assignment_count||0)?`Assigned ${m.assignment_count}`:'Unassigned'}</small><small>${esc(m.width_px&&m.height_px?`${m.width_px}×${m.height_px}`:fmtBytes(m.file_size_bytes))}</small></span></button>`).join('');el.querySelectorAll('[data-media-id]').forEach((b)=>b.addEventListener('click',()=>selectMedia(Number(b.dataset.mediaId))));}
  async function selectMedia(mediaId){state.selected=state.media.find(x=>Number(x.media_asset_id)===mediaId)||null;renderLibrary();renderSelected();if(!state.selected)return;try{const params=currentQuery();params.set('media_id',String(mediaId));const data=await read(await window.DDAuth.apiFetch(`/api/admin/media-content-studio?${params}`),'Could not load image uses.');state.uses=data.media_uses||[];state.slots=data.slots||state.slots;renderSelected();renderSlots();}catch(e){msg(e.message,'error');}}
  function setVal(key,value){const el=id(key);if(el)el.value=value??'';}
  function renderSelected(){const empty=id('mediaSelectedEmpty'),editor=id('mediaSelectedEditor'),m=state.selected;if(!m){empty.hidden=false;editor.hidden=true;id('mediaSelectedId').textContent='No selection';return;}empty.hidden=true;editor.hidden=false;id('mediaSelectedId').textContent=`Media #${m.media_asset_id}`;const img=id('mediaSelectedPreview');img.src=m.public_url||'/assets/mark.png';img.style.objectPosition=`${Math.round(Number(m.focal_x??.5)*100)}% ${Math.round(Number(m.focal_y??.5)*100)}%`;id('mediaSelectedTechnical').textContent=[m.original_filename||m.object_key,m.width_px&&m.height_px?`${m.width_px}×${m.height_px}`:'dimensions unknown',fmtBytes(m.file_size_bytes),m.product_name?`Product: ${m.product_name}`:''].filter(Boolean).join(' • ');setVal('mediaDisplayName',m.display_name||'');setVal('mediaEditorType',m.media_type||'photo');setVal('mediaAltText',m.alt_text||'');setVal('mediaImageTitle',m.image_title||'');setVal('mediaTags',(m.tags||[]).join(', '));setVal('mediaCaption',m.caption||'');setVal('mediaDescription',m.description||'');setVal('mediaSearchKeywords',m.search_keywords||'');setVal('mediaSourceType',m.source_type||'');setVal('mediaFocalX',m.focal_x??.5);setVal('mediaFocalY',m.focal_y??.5);id('mediaDecorative').checked=Number(m.decorative||0)===1;setVal('mediaAttribution',m.attribution||'');setVal('mediaCapturedAt',m.captured_at?String(m.captured_at).slice(0,10):'');setVal('mediaLicenseNotes',m.license_notes||'');setVal('mediaConsentNotes',m.consent_notes||'');renderTargets();renderUses();}
  function renderTargets(){const select=id('mediaAssignmentTarget');if(!select)return;const slots=state.slots.filter(s=>s.slot_type==='image'||s.slot_type==='background');select.innerHTML='<option value="">Choose a page image slot…</option>'+slots.map((s)=>`<option value="${s.media_content_slot_id}">${s.media_asset_id?'✓ ':''}${esc(s.slot_label)}${s.media_asset_id?` — ${esc(s.display_name||s.original_filename||'occupied')}`:' — empty'}</option>`).join('');}
  function renderUses(){const el=id('mediaExistingUses');const uses=state.uses||[];if(!uses.length){el.innerHTML='<p class="small">No active Media Studio assignments. The image may still be used by the authored product/catalog system.</p>';return;}el.innerHTML=uses.map(u=>`<div class="media-use-row"><div><strong>${esc(u.slot_label)}</strong><span class="small">${esc(u.page_path)} • ${esc(u.slot_type)}</span></div><div><a class="btn" target="_blank" href="${esc(u.page_path)}">Open page</a><button class="btn" type="button" data-remove-use="${u.media_content_slot_id}">Remove assignment</button></div></div>`).join('');el.querySelectorAll('[data-remove-use]').forEach(b=>b.addEventListener('click',()=>removeAssignment(Number(b.dataset.removeUse))));}
  function renderPages(){const el=id('mediaKnownPages');el.innerHTML=state.pages.length?state.pages.map(p=>`<button class="media-page-chip" type="button" data-path="${esc(p.page_path)}">${esc(p.page_path)} <span>${Number(p.slot_count||0)}</span></button>`).join(''):'<span class="small">No registered pages yet.</span>';el.querySelectorAll('[data-path]').forEach(b=>b.addEventListener('click',()=>{id('mediaPagePath').value=b.dataset.path;load();}));}
  function renderSlots(){const el=id('mediaPageSlots');if(!state.slots.length){el.innerHTML='<div class="media-empty-slot-guide"><strong>No registered slots for this page.</strong><p class="small">Click <b>Inspect page</b>, review what was found, then <b>Register inspected slots</b>. Registration alone does not alter the live page.</p></div>';return;}el.innerHTML=state.slots.map((s)=>{if(s.slot_type==='text'){return `<article class="media-slot-card text"><div class="media-slot-head"><div><strong>${esc(s.slot_label)}</strong><span class="small">Text • ${esc(s.target_selector)}</span></div><span class="media-slot-status ${s.published?'published':''}">${s.published?'Published override':'Authored text'}</span></div><div class="media-source-snapshot small"><b>Original:</b> ${esc(s.source_snapshot||'')}</div><label><span class="small">Draft replacement text</span><textarea class="input" rows="4" data-text-slot="${s.media_content_slot_id}">${esc(s.draft_text??s.source_snapshot??'')}</textarea></label><div class="media-slot-actions"><button class="btn" data-save-draft="${s.media_content_slot_id}">Save draft</button><button class="btn primary" data-publish-text="${s.media_content_slot_id}">Publish text</button>${s.published?`<button class="btn" data-unpublish-text="${s.media_content_slot_id}">Use authored text again</button>`:''}</div></article>`;}return `<article class="media-slot-card image"><div class="media-slot-head"><div><strong>${esc(s.slot_label)}</strong><span class="small">${esc(s.slot_type)} • ${esc(s.target_selector)}</span></div><span class="media-slot-status ${s.media_asset_id?'assigned':''}">${s.media_asset_id?'✓ Assigned':'Authored/fallback'}</span></div><div class="media-slot-preview"><div>${s.public_url?`<img src="${esc(s.public_url)}" alt=""/>`:'<div class="media-slot-placeholder">No Media Studio override</div>'}</div><div><p class="small"><b>Authored source:</b><br>${esc(s.source_snapshot||'not recorded')}</p>${s.media_asset_id?`<p class="small"><b>Assigned:</b> ${esc(s.display_name||s.original_filename||`Media ${s.media_asset_id}`)}</p>`:''}</div></div><div class="media-slot-actions"><button class="btn primary" data-assign-slot="${s.media_content_slot_id}" ${state.selected?'':'disabled'}>${state.selected?'Assign selected image':'Select an image first'}</button>${s.media_asset_id?`<button class="btn" data-remove-slot="${s.media_content_slot_id}">Remove override</button>`:''}</div></article>`;}).join('');
    el.querySelectorAll('[data-assign-slot]').forEach(b=>b.addEventListener('click',()=>assign(Number(b.dataset.assignSlot))));el.querySelectorAll('[data-remove-slot]').forEach(b=>b.addEventListener('click',()=>removeAssignment(Number(b.dataset.removeSlot))));el.querySelectorAll('[data-save-draft]').forEach(b=>b.addEventListener('click',()=>saveText(Number(b.dataset.saveDraft),false)));el.querySelectorAll('[data-publish-text]').forEach(b=>b.addEventListener('click',()=>saveText(Number(b.dataset.publishText),true)));el.querySelectorAll('[data-unpublish-text]').forEach(b=>b.addEventListener('click',()=>unpublishText(Number(b.dataset.unpublishText))));}
  function renderAll(){renderLibrary();renderSelected();renderPages();renderSlots();}
  function cssToken(v){return String(v||'').replace(/([^a-zA-Z0-9_-])/g,'\\$1');}
  function selectorFor(el){if(el.id)return `#${cssToken(el.id)}`;const parts=[];let node=el;while(node&&node.nodeType===1&&node.tagName.toLowerCase()!=='html'){const tag=node.tagName.toLowerCase();if(tag==='body'){parts.unshift('body');break;}const siblings=Array.from(node.parentElement?.children||[]).filter(x=>x.tagName===node.tagName);const nth=siblings.length>1?`:nth-of-type(${siblings.indexOf(node)+1})`:'';parts.unshift(`${tag}${nth}`);node=node.parentElement;if(parts.length>7)break;}return parts.join(' > ');}
  function hash(s){let h=2166136261;for(let i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619);}return (h>>>0).toString(36);}
  function humanLabel(el,type,index){const alt=text(el.getAttribute?.('alt'));const aria=text(el.getAttribute?.('aria-label'));const heading=text(el.textContent).replace(/\s+/g,' ').slice(0,80);if(type==='image')return alt?`Image — ${alt.slice(0,70)}`:`Image ${index+1}`;if(type==='background')return aria?`Background — ${aria}`:`Background ${index+1}`;return `${el.tagName.toUpperCase()} — ${heading||`Text ${index+1}`}`;}
  async function inspectPage(){const path=pathValue();if(path.startsWith('/admin')){msg('Media Studio registers public pages, not admin screens. Choose a public storefront path.','error');return;}id('mediaInspectorStatus').textContent='Inspecting the public page without changing it…';let frame=null;try{frame=document.createElement('iframe');frame.setAttribute('aria-hidden','true');frame.setAttribute('sandbox','allow-same-origin');frame.style.cssText='position:fixed;left:-12000px;top:0;width:1440px;height:1000px;opacity:0;pointer-events:none;';document.body.appendChild(frame);const loaded=new Promise((resolve,reject)=>{const timeout=setTimeout(()=>reject(new Error('Page inspection timed out.')),12000);frame.onload=()=>{clearTimeout(timeout);resolve();};frame.onerror=()=>{clearTimeout(timeout);reject(new Error('The public page could not be loaded for inspection.'));};});frame.src=path;await loaded;const doc=frame.contentDocument;if(!doc)throw new Error('The public page could not be read.');const win=frame.contentWindow;const found=[];Array.from(doc.querySelectorAll('img[src]')).slice(0,160).forEach((el,i)=>{const selector=selectorFor(el);if(!selector)return;found.push({slot_key:`image.${hash(selector)}`,slot_label:humanLabel(el,'image',i),slot_type:'image',target_selector:selector,target_attribute:'src',source_snapshot:el.getAttribute('src')||'',source_alt_snapshot:el.getAttribute('alt')||'',is_required:el.tagName==='IMG'&&!!el.closest('main h1')?1:0});});let bgIndex=0;for(const el of Array.from(doc.body?.querySelectorAll('*')||[]).slice(0,1200)){let background='';try{background=String(win?.getComputedStyle(el)?.backgroundImage||'');}catch{}if(!background||background==='none'||!background.includes('url('))continue;const selector=selectorFor(el);if(!selector)continue;found.push({slot_key:`background.${hash(selector)}`,slot_label:humanLabel(el,'background',bgIndex++),slot_type:'background',target_selector:selector,target_attribute:'background-image',source_snapshot:background.slice(0,1000),source_alt_snapshot:text(el.getAttribute?.('aria-label')),is_required:0});if(bgIndex>=100)break;}Array.from(doc.querySelectorAll('main h1,main h2,main h3,main p,main li,article h1,article h2,article h3,article p')).filter(el=>!el.closest('nav,footer,script,style,noscript')).slice(0,180).forEach((el,i)=>{const selector=selectorFor(el),value=text(el.textContent).replace(/\s+/g,' ');if(!selector||!value||value.length>5000)return;found.push({slot_key:`text.${hash(selector)}`,slot_label:humanLabel(el,'text',i),slot_type:'text',target_selector:selector,target_attribute:'textContent',source_snapshot:value,source_alt_snapshot:'',is_required:el.tagName==='H1'?1:0});});const unique=new Map(found.map(x=>[`${x.slot_type}:${x.target_selector}`,x]));state.inspected=Array.from(unique.values()).slice(0,300);id('mediaRegisterSlots').disabled=!state.inspected.length;id('mediaInspectorStatus').textContent=`Found ${state.inspected.filter(x=>x.slot_type==='image').length} images, ${state.inspected.filter(x=>x.slot_type==='background').length} image backgrounds and ${state.inspected.filter(x=>x.slot_type==='text').length} text locations. Review below; registration still does not change the live page.`;renderInspection();}catch(e){state.inspected=[];id('mediaRegisterSlots').disabled=true;id('mediaInspectorStatus').textContent='Inspection failed.';msg(`Could not inspect ${path}: ${e.message}`,'error');}finally{frame?.remove();}}
  function renderInspection(){const el=id('mediaInspectionPreview');if(!state.inspected.length){el.innerHTML='<p class="small">No locations found.</p>';return;}el.innerHTML=state.inspected.map(x=>`<div class="media-inspection-row"><span class="media-slot-type">${esc(x.slot_type)}</span><div><strong>${esc(x.slot_label)}</strong><small>${esc(x.target_selector)}</small></div><code>${esc(String(x.source_snapshot||'').slice(0,140))}</code></div>`).join('');}
  async function registerSlots(){if(!state.inspected.length)return;try{const data=await api({action:'register_slots',page_path:pathValue(),slots:state.inspected});msg(data.message,'success');state.slots=data.slots||[];state.inspected=[];id('mediaRegisterSlots').disabled=true;await load();}catch(e){msg(e.message,'error');}}
  async function saveMetadata(){if(!state.selected)return;try{const body={action:'save_media_metadata',media_asset_id:state.selected.media_asset_id,display_name:id('mediaDisplayName').value,media_type:id('mediaEditorType').value,alt_text:id('mediaAltText').value,image_title:id('mediaImageTitle').value,tags:id('mediaTags').value,caption:id('mediaCaption').value,description:id('mediaDescription').value,search_keywords:id('mediaSearchKeywords').value,source_type:id('mediaSourceType').value,focal_x:id('mediaFocalX').value,focal_y:id('mediaFocalY').value,decorative:id('mediaDecorative').checked,attribution:id('mediaAttribution').value,captured_at:id('mediaCapturedAt').value,license_notes:id('mediaLicenseNotes').value,consent_notes:id('mediaConsentNotes').value};const data=await api(body);msg(data.message,'success');await load();}catch(e){msg(e.message,'error');}}
  async function assign(slotId){if(!state.selected){msg('Select an image from the Media Library first.','error');return;}const slot=state.slots.find(s=>Number(s.media_content_slot_id)===Number(slotId));if(slot?.media_asset_id&&Number(slot.media_asset_id)!==Number(state.selected.media_asset_id)){if(!confirm(`“${slot.slot_label}” is occupied by ${slot.display_name||slot.original_filename||'another image'}. Replace only this placement with ${state.selected.display_name||state.selected.original_filename}?`))return;}try{const data=await api({action:'assign_media',media_content_slot_id:slotId,media_asset_id:state.selected.media_asset_id});msg(data.message,'success');await load();}catch(e){msg(e.message,'error');}}
  async function removeAssignment(slotId){if(!confirm('Remove this Media Studio override and return this placement to the image authored in the page/catalog?'))return;try{const data=await api({action:'remove_assignment',media_content_slot_id:slotId});msg(data.message,'success');await load();}catch(e){msg(e.message,'error');}}
  async function saveText(slotId,publish){const field=document.querySelector(`[data-text-slot="${slotId}"]`);if(!field)return;if(publish&&!confirm('Publish this replacement text to this exact page location?'))return;try{const data=await api({action:'save_content_block',media_content_slot_id:slotId,draft_text:field.value,publish});msg(data.message,'success');await load();}catch(e){msg(e.message,'error');}}
  async function unpublishText(slotId){if(!confirm('Stop using this text override and return to the authored page text?'))return;try{const data=await api({action:'unpublish_content',media_content_slot_id:slotId});msg(data.message,'success');await load();}catch(e){msg(e.message,'error');}}
  async function upload(){const file=id('mediaUploadFile').files?.[0];if(!file){msg('Choose an image file first.','error');return;}const form=new FormData();form.append('file',file);form.append('upload_scope',id('mediaUploadScope').value||'general');form.append('attach_to_product','0');form.append('variant_role','library');try{id('mediaUploadButton').disabled=true;const data=await read(await window.DDAuth.apiFetch('/api/admin/media-upload',{method:'POST',body:form}),'Image upload failed.');msg(`${data.message} It is in the library but not assigned to any page.`,'success');id('mediaUploadFile').value='';await load();if(data.asset?.media_asset_id)await selectMedia(Number(data.asset.media_asset_id));}catch(e){msg(e.message,'error');}finally{id('mediaUploadButton').disabled=false;}}
  async function replaceFile(){if(!state.selected)return;const file=id('mediaReplaceFile').files?.[0];if(!file){msg('Choose the replacement image file first.','error');return;}const password=prompt('Confirm the administrator password to replace this public image while keeping its placements.');if(password===null)return;const form=new FormData();form.append('media_asset_id',String(state.selected.media_asset_id));form.append('file',file);form.append('confirm_password',password);try{id('mediaReplaceButton').disabled=true;const data=await read(await window.DDAuth.apiFetch('/api/admin/media-content-replace',{method:'POST',body:form}),'Image replacement failed.');msg(data.message,'success');id('mediaReplaceFile').value='';await load();}catch(e){msg(e.message,'error');}finally{id('mediaReplaceButton').disabled=false;}}
  async function syncR2(){try{const data=await api({action:'sync_r2',prefix:id('mediaR2Prefix').value,cursor:state.r2Cursor||'',limit:100});state.r2Cursor=data.next_cursor||'';id('mediaR2Status').textContent=`${data.message}${data.truncated?' More objects remain; click again to continue.':' Sync page complete.'}`;msg(data.message,'success');await load();}catch(e){id('mediaR2Status').textContent=e.message;msg(e.message,'error');}}
  async function archive(){if(!state.selected)return;if(!confirm(`Archive ${state.selected.display_name||state.selected.original_filename}? Assigned images must be unassigned first.`))return;try{const data=await api({action:'archive_media',media_asset_id:state.selected.media_asset_id});msg(data.message,'success');state.selected=null;await load();}catch(e){msg(e.message,'error');}}
  async function del(){if(!state.selected)return;const phrase=`DELETE ${state.selected.media_asset_id}`;const confirmText=prompt(`Permanent deletion is allowed only when the image is unassigned. Type ${phrase} to delete it from public R2 and retire the library record.`);if(confirmText!==phrase)return;try{const data=await api({action:'delete_media',media_asset_id:state.selected.media_asset_id,confirm:confirmText});msg(data.message,'success');state.selected=null;await load();}catch(e){msg(e.message,'error');}}
  function bind(){id('mediaRefreshStudio').addEventListener('click',load);id('mediaReloadLibrary').addEventListener('click',load);id('mediaInspectPage').addEventListener('click',inspectPage);id('mediaRegisterSlots').addEventListener('click',registerSlots);id('mediaOpenPublicPage').addEventListener('click',()=>window.open(pathValue(),'_blank'));id('mediaPagePath').addEventListener('keydown',(e)=>{if(e.key==='Enter'){e.preventDefault();load();}});let timer;id('mediaSearch').addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(load,300);});id('mediaTypeFilter').addEventListener('change',load);id('mediaAssignmentFilter').addEventListener('change',load);id('mediaSaveMetadata').addEventListener('click',saveMetadata);id('mediaAssignSelected').addEventListener('click',()=>{const slotId=Number(id('mediaAssignmentTarget').value||0);if(slotId)assign(slotId);else msg('Choose a registered image placement first.','error');});id('mediaUploadButton').addEventListener('click',upload);id('mediaReplaceButton').addEventListener('click',replaceFile);id('mediaSyncR2').addEventListener('click',syncR2);id('mediaArchive').addEventListener('click',archive);id('mediaDelete').addEventListener('click',del);document.querySelectorAll('.mediaPresetFilter').forEach(b=>b.addEventListener('click',()=>{id('mediaTypeFilter').value=b.dataset.type||'';load();}));}
  async function start(){const params=new URLSearchParams(location.search);if(params.get('path'))id('mediaPagePath').value=params.get('path');if(params.get('media_type'))id('mediaTypeFilter').value=params.get('media_type');bind();await load();}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();
