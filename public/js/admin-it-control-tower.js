// Release 467 Build 51 — Explicit Content Studio Schema Readiness renderer.
document.addEventListener('DOMContentLoaded', () => {
  const mount = document.getElementById('itControlTowerMount');
  if (!mount) return;
  const esc = (value) => String(value ?? '').replace(/[&<>"']/g, (ch) => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
  const apiFetch = (...args) => window.DDAuth?.apiFetch ? window.DDAuth.apiFetch(...args) : fetch(...args);
  const goodState = new Set(['green','DEVELOPMENT_GREEN','PRODUCTION_GREEN','EXACT_BRANCH_HEAD_FOUR_PROOF_GREEN']);
  const stateClass = (value) => goodState.has(String(value || '')) ? 'good' : String(value || '').toLowerCase() === 'red' ? 'bad' : 'warn';
  const stateLabel = (value) => String(value || 'unknown').toUpperCase();
  const shortSha = (value) => /^[0-9a-f]{40}$/i.test(String(value || '')) ? String(value).slice(0, 12) : (value || 'unavailable');
  const card = (label, lines=[]) => `<div class="card" style="padding:14px"><strong>${esc(label)}</strong>${lines.map((line)=>`<div class="small">${esc(line)}</div>`).join('')}</div>`;
  function findingRow(item) { return `<div class="card" style="margin:10px 0;padding:14px"><div style="display:flex;justify-content:space-between;gap:12px;align-items:flex-start;flex-wrap:wrap"><div><strong>${esc(item?.label || item?.code || 'Readiness check')}</strong><div class="small">${esc(item?.detail || '')}</div></div><span class="badge ${stateClass(item?.state)}">${esc(stateLabel(item?.state))}</span></div>${item?.correction ? `<div class="small" style="margin-top:8px"><strong>Correction:</strong> ${esc(item.correction)}</div>` : ''}<a class="small" href="${esc(item?.href || '/admin/it/')}">Open corrective workspace →</a></div>`; }
  function subsystemCard(key, item) { const labels={database:'D1 & migration authority',admin_authority:'Administrator & module authority',storage:'R2 storage authority',configuration:'Development configuration',provider_configuration:'Provider configuration',external_acceptance:'External acceptance evidence',deployment_ancestry:'Exact-SHA / deployment ancestry'}; const findings=Array.isArray(item?.findings)?item.findings:[]; return `<section class="card" style="margin-top:14px"><div style="display:flex;justify-content:space-between;gap:12px;align-items:center;flex-wrap:wrap"><h3 style="margin:0">${esc(labels[key]||key)}</h3><span class="badge ${stateClass(item?.state)}">${esc(stateLabel(item?.state))}</span></div>${findings.map(findingRow).join('')||'<p class="small">No findings returned.</p>'}</section>`; }
  function recoverySection(queue) { const items=Array.isArray(queue)?queue:[]; if(!items.length) return `<section class="card" style="margin-top:14px"><h3 style="margin-top:0">Prioritized recovery queue</h3><p class="small">No blocking or attention findings are currently reported by the read-only preflight engine.</p></section>`; return `<section class="card" style="margin-top:14px"><h3 style="margin-top:0">Prioritized recovery queue</h3><p class="small">This queue routes work; it never performs repairs automatically.</p>${items.map((item,index)=>`<div class="card" style="margin:10px 0;padding:14px"><strong>${index+1}. ${esc(item.label)}</strong><div class="small">${esc(item.subsystem)} • ${esc(item.detail||'')}</div><div class="small"><strong>Do next:</strong> ${esc(item.correction||'')}</div><a class="small" href="${esc(item.href||'/admin/it/')}">Open corrective workspace →</a></div>`).join('')}</section>`; }
  function externalPolicySection(policy) { const rows=Array.isArray(policy)?policy:[]; return `<section class="card" style="margin-top:14px"><h3 style="margin-top:0">External acceptance policy</h3><p class="small">These states require their own current evidence. A green source, database or deployment build never promotes an external HOLD automatically.</p><div class="admin-compact-tool-grid">${rows.map((item)=>`<div><strong>${esc(item.label)}</strong><small>${esc(item.state)} • ${esc(item.authority)}</small><a class="small" href="${esc(item.href||'/admin/it/')}">Open authority →</a></div>`).join('')}</div></section>`; }
  async function load(){
    mount.innerHTML='<section class="card" style="margin-top:18px"><p class="small">Running current I.T. release/deployment truth preflight…</p></section>';
    try{
      const response=await apiFetch('/api/admin/it-operations-control-tower',{cache:'no-store'}); const data=await response.json().catch(()=>({})); if(!response.ok||!data?.ok) throw new Error(data?.error||`I.T. control tower failed (${response.status}).`);
      const ready=data.readiness||{}, metrics=data.headline_metrics||{}, release=data.release_authority||{}, current=release.current_operator||{}, accepted=release.accepted_development||{}, verified=release.verified_development||{}, production=release.production||{}, development=data.development||{}, subsystems=data.subsystems||{};
      const ordered=['database','admin_authority','storage','configuration','provider_configuration','external_acceptance','deployment_ancestry'];
      mount.innerHTML=`<section class="card" style="margin-top:18px"><div style="display:flex;justify-content:space-between;align-items:flex-start;gap:16px;flex-wrap:wrap"><div><p class="eyebrow">Release ${esc(current.release||data.release)} • Build ${esc(current.build||data.build)}</p><h2 style="margin:0">I.T. Current Release & Production Truth</h2><p class="small">Current Development operator truth, accepted implementation evidence, last fully verified restart checkpoint, current Reliability and Deployment Preflight authority, release-neutral promotion/rollback provenance, exact Production authority, D1/R2 readiness and external HOLDs.</p></div><div style="text-align:right"><div style="font-size:2rem;font-weight:800">${esc(metrics.readiness_score??ready.score??0)}%</div><span class="badge ${stateClass(current.state)}">${esc(stateLabel(current.state))}</span></div></div><div class="admin-compact-tool-grid" style="margin-top:14px">${card('Current operator authority',[`R${current.release||data.release} B${current.build||data.build} • ${current.state||data.state}`,`Runtime deployed SHA: ${shortSha(development.runtime_source_sha)}`])}${card('Accepted implementation',[`Build ${accepted.build||'?'} • ${shortSha(accepted.accepted_sha||accepted.accepted_dev_sha)}`,`System ${accepted.system_gate_run||'unknown'} • Quality ${accepted.current_application_quality_run||'unknown'} • I.T. ${accepted.it_admin_runtime_proof_run||'unknown'} • Hygiene ${accepted.branch_hygiene_run||'unknown'}`])}${card('Last fully verified restart checkpoint',[`Build ${verified.build||'?'} • ${shortSha(verified.dev_sha)}`,`System ${verified.system_gate_run||'unknown'} • Quality ${verified.current_application_quality_run||'unknown'} • I.T. ${verified.it_admin_runtime_proof_run||'unknown'} • Hygiene ${verified.branch_hygiene_run||'unknown'}`])}${card('Production GREEN authority',[`Build ${production.build||'?'} • main ${shortSha(production.main_sha)}`,`Tree ${shortSha(production.tree_sha)} • Deploy ${production.pages_deploy_run||'unknown'}`])}</div><div class="admin-compact-tool-grid" style="margin-top:14px"><div><strong>Root admin</strong><small>${metrics.root_admin_full_manage?'FULL MANAGE':'HOLD'}</small></div><div><strong>Profiles / admins</strong><small>${esc(metrics.active_profiles??'?')} / ${esc(metrics.active_admins??'?')}</small></div><div><strong>Enabled modules</strong><small>${esc(metrics.enabled_modules??'?')} / 5</small></div><div><strong>D1 tables</strong><small>${esc(metrics.d1_tables??'unknown')}</small></div><div><strong>Migrations / proofs</strong><small>${esc(metrics.canonical_migrations??'?')} / ${esc(metrics.migration_proofs??'?')}</small></div><div><strong>FK violations</strong><small>${esc(metrics.foreign_key_violations??'unknown')}</small></div><div><strong>Blocking / attention</strong><small>${esc(metrics.blocking_actions??0)} / ${esc(metrics.attention_actions??0)}</small></div></div><div style="margin-top:14px;display:flex;gap:10px;flex-wrap:wrap"><button class="btn" id="refreshItControlTower" type="button">Refresh control tower</button><a class="btn secondary" href="/admin/reliability/">Reliability</a><a class="btn secondary" href="/admin/application-modules/">Application Modules</a><a class="btn secondary" href="/admin/release-control/external-commercial-readiness/">External acceptance</a><a class="btn secondary" href="/admin/deployment-preflight/">Deployment preflight</a></div><p class="small" style="margin-bottom:0;margin-top:12px">Development: ${esc(development.target||'')} • D1 ${esc(development.d1_name||'')} • R2 ${esc(development.product_r2||'')} / ${esc(development.caip_r2||'')}</p><p class="small" style="margin-bottom:0">Persistent branches: ${esc((release.persistent_branches||[]).join(', '))} • Current automatic guards: ${esc((release.current_automatic_guards||[]).join(' • '))}</p></section>${recoverySection(data.recovery_queue)}${externalPolicySection(data.external_policy)}<section class="card" style="margin-top:14px"><h3 style="margin-top:0">Truth boundaries</h3>${(data.truth_notes||[]).map((note)=>`<p class="small">• ${esc(note)}</p>`).join('')}</section>${ordered.map((key)=>subsystemCard(key,subsystems[key]||{state:'amber',findings:[]})).join('')}`;
      document.getElementById('refreshItControlTower')?.addEventListener('click',load);
    }catch(error){ mount.innerHTML=`<section class="card" style="margin-top:18px"><h2>I.T. Control Tower unavailable</h2><p class="small">${esc(error?.message||'Unable to load the control tower.')}</p><button class="btn" id="retryItControlTower" type="button">Retry</button></section>`; document.getElementById('retryItControlTower')?.addEventListener('click',load); }
  }
  void load();
});
