// File: /public/js/admin-trust-block-placement-preview.js
// Brief description: Trust-block placement preview helper on /admin/trust-blocks/.
document.addEventListener('DOMContentLoaded',()=>{const mount=document.getElementById('trustBlockPlacementPreviewMount');if(!mount||!window.DDAuth)return;const esc=(v)=>String(v??'').replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));mount.innerHTML='<h2>Placement preview</h2><p class="small">Preview candidate proof placement before promotion from moderation cards.</p><label class="small">Candidate ID<input id="trustPreviewCandidateId" type="number"></label><label class="small">Context<input id="trustPreviewContext" value="sitewide"></label><button class="btn" id="trustPreviewButton" type="button">Preview</button><div id="trustPreviewResults" class="small"></div>';document.getElementById('trustPreviewButton')?.addEventListener('click',async()=>{const id=Number(document.getElementById('trustPreviewCandidateId')?.value||0);const ctx=document.getElementById('trustPreviewContext')?.value||'sitewide';const box=document.getElementById('trustPreviewResults');try{const r=await window.DDAuth.apiFetch('/api/admin/public-proof-candidates',{method:'POST',body:JSON.stringify({action:'promotion_preview',public_proof_candidate_id:id,target_contexts:[ctx]})});const d=await r.json();if(!r.ok||!d?.ok)throw new Error(d?.error||'Preview failed.');box.innerHTML=`Can promote: ${d.preview?.can_promote?'yes':'no'}<br>Placements: ${(d.preview?.placements||[]).map(p=>esc(p.page_path||p.placement_context)).join(', ')||'none found'}`}catch(e){box.textContent=e.message}})});
