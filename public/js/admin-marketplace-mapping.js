(function(){const mount=document.getElementById('marketplaceMappingMount'); if(!mount)return; const esc=(v)=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])); async function api(path,opt={}){const f=window.DDAuth?.apiFetch||fetch; const r=await f(path,opt); const d=await r.json().catch(()=>null); if(!r.ok||!d?.ok)throw new Error(d?.error||'Request failed.'); return d} function render(data){mount.innerHTML=`<div style="display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap"><div><h2 style="margin:0">CSV mappings</h2><p class="small">Use comma-separated column names. These mappings feed marketplace export previews and downloads.</p></div><a class="btn" href="/admin/marketplace-exports/">Back to export preview</a></div><div id="mappingMsg" class="small" style="display:none;margin-top:10px"></div><div class="marketplace-mapping-grid" style="margin-top:12px">${(data.mappings||[]).map(row=>`<form class="marketplace-mapping-card" data-channel="${esc(row.channel)}"><h3 style="margin-top:0;text-transform:capitalize">${esc(row.channel)}</h3><label><span class="small">Columns</span><textarea rows="5" data-field="columns">${esc((JSON.parse(row.mapping_json||'[]')||[]).join(', '))}</textarea></label><button class="btn" type="submit">Save ${esc(row.channel)}</button></form>`).join('')}</div>`} async function load(){try{render(await api('/api/admin/marketplace-csv-mapping'))}catch(e){mount.innerHTML=`<div class="small">${esc(e.message)}</div>`}} mount.addEventListener('submit',async(e)=>{const form=e.target.closest('[data-channel]'); if(!form)return; e.preventDefault(); const columns=String(form.querySelector('[data-field="columns"]')?.value||'').split(/[\r\n,;|]+/).map(s=>s.trim()).filter(Boolean); const msg=document.getElementById('mappingMsg'); try{const out=await api('/api/admin/marketplace-csv-mapping',{method:'POST',body:JSON.stringify({channel:form.dataset.channel,mapping_json:columns})}); if(msg){msg.style.display='';msg.textContent=out.message||'Saved.'}}catch(err){if(msg){msg.style.display='';msg.textContent=err.message||'Save failed.'}}}); load();})();
