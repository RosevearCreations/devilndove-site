// Build 220 — purchase-lot editor for repeated purchases of the same material.
(() => {
  const esc=(value)=>String(value??'').replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const money=(c)=>new Intl.NumberFormat(undefined,{style:'currency',currency:'CAD'}).format(Number(c||0)/100);
  let selectedItemId=0, detail=null;
  function mount(){let node=document.getElementById('inventoryLotsAdminMount');if(node)return node;const host=document.getElementById('siteInventoryAdminMount');if(!host)return null;node=document.createElement('section');node.id='inventoryLotsAdminMount';node.className='card inventory-lots-admin';node.style.marginTop='16px';host.parentNode?.insertBefore(node,host.nextSibling);return node;}
  function msg(text='',error=false){const n=document.getElementById('inventoryLotsMessage');if(!n)return;n.textContent=text;n.hidden=!text;n.className=`small ${error?'is-error':'is-success'}`;}
  async function load(itemId){selectedItemId=Number(itemId||0);detail=null;render();if(!selectedItemId)return;try{const r=await DDAuth.apiFetch(`/api/admin/inventory-lots?site_item_inventory_id=${encodeURIComponent(selectedItemId)}`);const d=await r.json();if(!r.ok||!d?.ok)throw new Error(d?.error||'Purchase lots could not load.');detail=d.detail;render();}catch(e){msg(e.message,true)}}
  function row(lot){return `<tr><td>${esc(lot.lot_code)}</td><td>${esc(lot.purchase_date||'—')}</td><td>${esc(lot.supplier_name||'—')}<div class="small">${esc(lot.supplier_order_number||'')}</div></td><td>${lot.quantity_remaining} / ${lot.quantity_received}</td><td>${money(lot.unit_cost_cents)}</td><td>${esc(lot.expiry_date||'—')}</td><td>${esc(lot.lot_status||'available')}</td><td><button class="btn" type="button" data-edit-lot='${esc(JSON.stringify(lot))}'>Edit</button><button class="btn danger" type="button" data-delete-lot="${lot.inventory_purchase_lot_id}">Delete</button></td></tr>`;}
  function render(){const node=mount();if(!node)return;if(!selectedItemId){node.innerHTML=`<h2 style="margin-top:0">Purchase lots & batches</h2><p class="small">Use <strong>Lots</strong> beside an inventory row to separate repeated Amazon or supplier purchases by date, order, cost, remaining quantity, expiry, and storage location.</p>`;return;}if(!detail){node.innerHTML=`<h2 style="margin-top:0">Purchase lots & batches</h2><p class="small">Loading selected inventory item…</p><div id="inventoryLotsMessage" hidden></div>`;return;}const item=detail.item||{},lots=detail.lots||[],s=detail.summary||{};node.innerHTML=`<div class="section-heading-row"><div><h2 style="margin:0">Lots for ${esc(item.item_name)}</h2><p class="small">${lots.length} purchase lot(s) · ${s.quantity_remaining||0} ${esc(item.stock_unit_label||'unit')} recorded across lots. Lot totals are evidence and do not silently replace the main on-hand count.</p></div><button class="btn" id="closeInventoryLots" type="button">Close lots</button></div><div id="inventoryLotsMessage" hidden></div><div class="inventory-lot-intro"><img src="/assets/inventory-lot-tracking-placeholder.svg" alt="Purchase lots separated by purchase date, supplier, cost and remaining quantity"/><p class="small">Create one lot for each purchase occasion so goat milk base, oils, mica and coloured bases remain traceable by supplier order and date.</p></div>
  <form id="inventoryLotForm" class="inventory-lot-form"><input type="hidden" id="inventoryPurchaseLotId"/><div class="grid cols-4"><label><span class="small">Lot / batch code</span><input class="input" id="inventoryLotCode" required placeholder="AMZ-2026-07-01-A"/></label><label><span class="small">Purchase date</span><input class="input" id="inventoryLotPurchaseDate" type="date"/></label><label><span class="small">Received date</span><input class="input" id="inventoryLotReceivedDate" type="date"/></label><label><span class="small">Status</span><select class="input" id="inventoryLotStatus"><option>available</option><option>consumed</option><option>expired</option><option>quarantined</option><option>returned</option></select></label></div><div class="grid cols-4"><label><span class="small">Supplier</span><input class="input" id="inventoryLotSupplier" value="${esc(item.supplier_name||'')}"/></label><label><span class="small">Order number</span><input class="input" id="inventoryLotOrder"/></label><label><span class="small">Supplier SKU / ASIN</span><input class="input" id="inventoryLotSku" value="${esc(item.supplier_sku||'')}"/></label><label><span class="small">Source URL</span><input class="input" id="inventoryLotUrl" type="url" value="${esc(item.amazon_url||item.source_url||'')}"/></label></div><div class="grid cols-4"><label><span class="small">Quantity received</span><input class="input" id="inventoryLotReceivedQty" type="number" min="0" step="0.01" value="1"/></label><label><span class="small">Quantity remaining</span><input class="input" id="inventoryLotRemainingQty" type="number" min="0" step="0.01" value="1"/></label><label><span class="small">Unit cost before tax (CAD)</span><input class="input" id="inventoryLotUnitCost" type="number" min="0" step="0.01" value="${(Number(item.unit_cost_cents||0)/100).toFixed(2)}"/></label><label><span class="small">Expiry / best-before</span><input class="input" id="inventoryLotExpiry" type="date"/></label></div><div class="grid cols-2"><label><span class="small">Shipping allocated to this lot (CAD)</span><input class="input" id="inventoryLotShippingCost" type="number" min="0" step="0.01" value="0.00"/></label><label><span class="small">Tax allocated to this lot (CAD)</span><input class="input" id="inventoryLotTaxCost" type="number" min="0" step="0.01" value="0.00"/></label></div><div class="grid cols-2"><label><span class="small">Storage location</span><input class="input" id="inventoryLotStorage" placeholder="Soap shelf A / sealed tote 2"/></label><label><span class="small">Notes</span><input class="input" id="inventoryLotNotes" placeholder="Colour, formulation, packaging condition, intended project"/></label></div><div class="product-offer-actions"><button class="btn primary" type="submit">Save purchase lot</button><button class="btn" type="button" id="resetInventoryLotForm">Clear form</button></div></form>
  <div class="admin-table-wrap"><table class="inventory-lot-table"><thead><tr><th>Lot</th><th>Purchased</th><th>Supplier / order</th><th>Remaining</th><th>Unit cost</th><th>Expiry</th><th>Status</th><th>Actions</th></tr></thead><tbody>${lots.length?lots.map(row).join(''):'<tr><td colspan="8">No lots recorded yet.</td></tr>'}</tbody></table></div>`;bind();}
  function values(){return{inventory_purchase_lot_id:Number(document.getElementById('inventoryPurchaseLotId')?.value||0)||undefined,site_item_inventory_id:selectedItemId,lot_code:document.getElementById('inventoryLotCode')?.value||'',purchase_date:document.getElementById('inventoryLotPurchaseDate')?.value||'',received_date:document.getElementById('inventoryLotReceivedDate')?.value||'',supplier_name:document.getElementById('inventoryLotSupplier')?.value||'',supplier_order_number:document.getElementById('inventoryLotOrder')?.value||'',supplier_sku:document.getElementById('inventoryLotSku')?.value||'',asin:document.getElementById('inventoryLotSku')?.value||'',source_url:document.getElementById('inventoryLotUrl')?.value||'',quantity_received:Number(document.getElementById('inventoryLotReceivedQty')?.value||0),quantity_remaining:Number(document.getElementById('inventoryLotRemainingQty')?.value||0),unit_cost_cents:Math.round(Number(document.getElementById('inventoryLotUnitCost')?.value||0)*100),shipping_cost_cents:Math.round(Number(document.getElementById('inventoryLotShippingCost')?.value||0)*100),tax_cost_cents:Math.round(Number(document.getElementById('inventoryLotTaxCost')?.value||0)*100),expiry_date:document.getElementById('inventoryLotExpiry')?.value||'',storage_location:document.getElementById('inventoryLotStorage')?.value||'',lot_status:document.getElementById('inventoryLotStatus')?.value||'available',notes:document.getElementById('inventoryLotNotes')?.value||''};}
  function populate(lot){const map={inventoryPurchaseLotId:lot.inventory_purchase_lot_id,inventoryLotCode:lot.lot_code,inventoryLotPurchaseDate:lot.purchase_date,inventoryLotReceivedDate:lot.received_date,inventoryLotSupplier:lot.supplier_name,inventoryLotOrder:lot.supplier_order_number,inventoryLotSku:lot.supplier_sku||lot.asin,inventoryLotUrl:lot.source_url,inventoryLotReceivedQty:lot.quantity_received,inventoryLotRemainingQty:lot.quantity_remaining,inventoryLotUnitCost:(Number(lot.unit_cost_cents||0)/100).toFixed(2),inventoryLotShippingCost:(Number(lot.shipping_cost_cents||0)/100).toFixed(2),inventoryLotTaxCost:(Number(lot.tax_cost_cents||0)/100).toFixed(2),inventoryLotExpiry:lot.expiry_date,inventoryLotStorage:lot.storage_location,inventoryLotStatus:lot.lot_status,inventoryLotNotes:lot.notes};Object.entries(map).forEach(([id,value])=>{const n=document.getElementById(id);if(n)n.value=value??''});document.getElementById('inventoryLotForm')?.scrollIntoView({behavior:'smooth',block:'start'});}
  function bind(){document.getElementById('closeInventoryLots')?.addEventListener('click',()=>load(0));document.getElementById('resetInventoryLotForm')?.addEventListener('click',()=>{document.getElementById('inventoryLotForm')?.reset();const id=document.getElementById('inventoryPurchaseLotId');if(id)id.value='';});document.getElementById('inventoryLotForm')?.addEventListener('submit',async(e)=>{e.preventDefault();try{msg('Saving purchase lot…');const r=await DDAuth.apiFetch('/api/admin/inventory-lots',{method:'POST',body:JSON.stringify(values())});const d=await r.json();if(!r.ok||!d?.ok)throw new Error(d?.error||'Purchase lot could not be saved.');detail=d.detail;render();msg(d.message);}catch(error){msg(error.message,true)}});document.querySelectorAll('[data-edit-lot]').forEach((b)=>b.onclick=()=>{try{populate(JSON.parse(b.dataset.editLot||'{}'))}catch{}});document.querySelectorAll('[data-delete-lot]').forEach((b)=>b.onclick=async()=>{if(!confirm('Delete this lot record? This does not change the main on-hand count.'))return;try{const r=await DDAuth.apiFetch(`/api/admin/inventory-lots?inventory_purchase_lot_id=${encodeURIComponent(b.dataset.deleteLot)}`,{method:'DELETE'});const d=await r.json();if(!r.ok||!d?.ok)throw new Error(d?.error||'Lot could not be deleted.');await load(selectedItemId);msg(d.message);}catch(error){msg(error.message,true)}});}
  document.addEventListener('click',(event)=>{const b=event.target.closest('[data-open-inventory-lots]');if(!b)return;load(Number(b.dataset.openInventoryLots||0));setTimeout(()=>mount()?.scrollIntoView({behavior:'smooth',block:'start'}),80);});
  document.addEventListener('DOMContentLoaded',()=>{mount();render();});
})();
