// Devil n Dove Release 448 — durable Tool lifecycle workspace.
(()=>{'use strict';const $=id=>document.getElementById(id),text=v=>String(v??'').trim(),num=v=>Number(v||0)||0,esc=v=>text(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const state={tools:[],id:0,detail:null};async function api(url,options={}){if(!window.DDAuth?.apiFetch)throw new Error('Authentication helper is unavailable.');const r=await window.DDAuth.apiFetch(url,options),d=await r.json().catch(()=>({}));if(!r.ok||d.ok===false)throw new Error(d.error||`HTTP ${r.status}`);return d}function msg(v){$('tlMessage').textContent=v||''}function renderList(){const q=text($('tlSearch').value).toLowerCase();$('tlList').innerHTML=state.tools.filter(t=>!q||[t.item_name,t.category,t.external_key].join(' ').toLowerCase().includes(q)).map(t=>`<button type="button" class="tl-tool" data-id="${num(t.site_item_inventory_id)}" aria-current="${num(t.site_item_inventory_id)===state.id?'true':'false'}"><strong>${esc(t.item_name||t.external_key)}</strong><div class="small">${esc(t.condition_status||'unverified')} • ${esc(t.lifecycle_status||'active')} • ${num(t.linked_product_count)} Product link(s)${t.next_service_at?` • next ${esc(t.next_service_at)}`:''}</div></button>`).join('')||'<p class="small">No matching Tools.</p>';$('tlList').querySelectorAll('[data-id]').forEach(b=>b.addEventListener('click',()=>select(num(b.dataset.id))))}function setForm(form,data={}){[...form.elements].forEach(el=>{if(!el.name)return;el.value=data[el.name]??''})}function renderDetail(){const d=state.detail;if(!d)return;$('tlEmpty').hidden=true;$('tlDetail').hidden=false;$('tlName').textContent=d.item.item_name||d.item.external_key;$('tlMeta').textContent=`Inventory #${d.item.site_item_inventory_id} • ${d.item.category||'uncategorized'} • on hand ${num(d.item.on_hand_quantity)} • quantity is not changed by lifecycle events`;setForm($('tlProfile'),{lifecycle_status:d.profile?.lifecycle_status||'active',condition_status:d.profile?.condition_status||'unverified',last_service_at:text(d.profile?.last_service_at).slice(0,10),next_service_at:text(d.profile?.next_service_at).slice(0,10),service_interval_days:d.profile?.service_interval_days||'',replacement_priority:d.profile?.replacement_priority||'normal',replacement_cost_cents:d.profile?.replacement_cost_cents??'',replacement_site_item_inventory_id:d.profile?.replacement_site_item_inventory_id??'',evidence_reference:d.profile?.evidence_reference||'',notes:d.profile?.notes||''});$('tlEvents').innerHTML=(d.events||[]).map(e=>`<div class="tl-event"><strong>${esc(e.event_type)}</strong> <span class="small">${esc(e.occurred_at)}</span><div class="small">${esc(e.condition_before||'—')} → ${esc(e.condition_after||'—')}${e.service_cost_cents!=null?` • ${(num(e.service_cost_cents)/100).toLocaleString('en-CA',{style:'currency',currency:'CAD'})}`:''}</div>${e.notes?`<div class="small">${esc(e.notes)}</div>`:''}</div>`).join('')||'<p class="small">No lifecycle events recorded.</p>';$('tlProducts').innerHTML=(d.product_links||[]).map(p=>`<div class="tl-event"><strong>${esc(p.name)}</strong><div class="small">${esc(p.status||'')} ${p.usage_notes?`• ${esc(p.usage_notes)}`:''}</div></div>`).join('')||'<p class="small">No Product resource links currently use this Tool.</p>';renderList()}async function loadTools(){const d=await api('/api/admin/tool-lifecycle',{cache:'no-store'});state.tools=Array.isArray(d.tools)?d.tools:[];if(d.schema_ready===false)msg('Release 448 Tool lifecycle D1 schema is not installed yet.');renderList()}async function select(id){state.id=id;msg('Loading Tool lifecycle…');const d=await api(`/api/admin/tool-lifecycle?site_item_inventory_id=${id}`,{cache:'no-store'});state.detail=d.detail;renderDetail();msg('Tool lifecycle loaded.')}async function submit(form,action){if(!state.id)return;const body=Object.fromEntries(new FormData(form).entries());body.action=action;body.site_item_inventory_id=state.id;const d=await api('/api/admin/tool-lifecycle',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});state.detail=d.detail;renderDetail();msg(d.message||'Saved.')}function init(){$('tlSearch')?.addEventListener('input',renderList);$('tlProfile')?.addEventListener('submit',e=>{e.preventDefault();submit(e.currentTarget,'save_profile').catch(x=>msg(x.message))});$('tlEvent')?.addEventListener('submit',e=>{e.preventDefault();submit(e.currentTarget,'record_event').then(()=>e.currentTarget.reset()).catch(x=>msg(x.message))});loadTools().catch(x=>msg(x.message))}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()})();